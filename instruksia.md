# 📖 Инструкция и Правила Проекта (Instruksia)

Этот файл служит единым источником правды о специфике проекта, правилах разработки и архитектурных решениях.
**Агент:** Перед началом сложной задачи сверься с этим файлом. При изменениях в архитектуре или новых договоренностях — обновляй его.
Отвечай только на русском языке ! 
Так же постоянно объновляй этот фаил чтобы он был всегда актуальный ! 

## 📌 Правила
1. **Backend:** Все API-эндпоинты должны быть документированы в Swagger (http://localhost:8000/docs).
2. **Frontend:** Используем React + Vite (TypeScript). Компоненты должны быть модульными (CSS Modules).
3. **Database:** Локально используем SQLite (`construction_costs.db`). В продакшене — PostgreSQL (Docker).
4. **Telegram Bot:** Запускается отдельным процессом (не в FastAPI). Логика бота — в `app/bot/main.py`.
5. **Docker:** Используем только для продакшена. Локально — чистый Python + npm.
6. **Git:** Все коммиты должны быть осмысленными. Используем Conventional Commits (feat: add new endpoint, fix: bug in calculation).
7. **Общение:** Все ответы — только на русском. Если нужно что-то уточнить — спрашивай.
8. **Обновление файла:** При любых изменениях в архитектуре или правилах — обновляй этот файл.



## 🛠 Технический Стек
- **Backend:** FastAPI (Python), `.venv`
- **Frontend:** React + Vite (TypeScript), Tailwind (если используется), CSS Modules.
- **Database (Local):** SQLite (`construction_costs.db`) — используется по умолчанию для локальной разработки.
- **Database (Prod):** PostgreSQL (запускается через Docker, но локально часто пропускаем).
- **Инфраструктура:** Docker Compose (Postgres, Redis, MinIO).

## 🚀 Запуск Проекта (Local Dev)
> **Правило:** При просьбе "запусти сервера" используем этот алгоритм, если не сказано иначе. Docker не трогаем, если не просили.

1. **Backend:**
   ```powershell
   cd backend
   .venv\Scripts\Activate.ps1
   python main.py
   # Запускается на http://localhost:8000
   ```

2. **Telegram Bot:**
   ```powershell
   cd backend
   .venv\Scripts\Activate.ps1
   python create_test_data.py
   python seed_estimates.py # Заполнение смет (материалы)
   ```

3. **Frontend:**
   ```powershell
   cd frontend
   cd frontend
   npm run dev
   # Запускается на http://localhost:3000
   ```

## 🏗 Архитектура и "Тонкости"
### 1. Mini App (Telegram Web App)
Проект содержит "Mini App" для прорабов и менеджеров.
- **Пути для Прораба:**
  - Выбор объекта: `/miniapp/objects`
  - Мои заявки: `/miniapp/my-requests`
  - Просмотр сметы: `/miniapp/estimate/:objectId`
- **Пути для Менеджера:**
  - Дашборд: `/miniapp/manager`
  - Табели (TimeSheets): `/miniapp/timesheets`

### 2. База Данных
- Локально используем **SQLite** (`DATABASE_URL=sqlite+aiosqlite:///./construction_costs.db` в `.env`).
- Не пытаться запускать Postgres через Docker, если пользователь явно не попросил "поднять полную инфраструктуру".

### 3. Стиль Кода и UI
- Интерфейс должен быть богатым ("Rich Aesthetics"), избегать дефолтных стилей браузера.
- Использовать анимации и качественные отступы.
- **Числа и форматирование:** При отображении количеств (особенно дробных) на фронтенде ВСЕГДА используйте `toLocaleString` с ограничением до 3 знаков (`maximumFractionDigits: 3`). Это критично для устранения ошибок плавающей точки.

## 📝 TODO / Актуальные Заметки
- [ ] Исправить скрипт `scripts/start-infrastructure.ps1` (синтаксическая ошибка).
- [ ] Проверить работу роутинга Mini App на реальных устройствах.
