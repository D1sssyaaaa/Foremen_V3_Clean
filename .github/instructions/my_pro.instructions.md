---
applyTo: '**'
---

# СИСТЕМА УЧЕТА ЗАТРАТ СТРОИТЕЛЬНОЙ КОМПАНИИ

## КОНТЕКСТ ПРОЕКТА

Единая платформа управленческого учета затрат строительной компании с распределением по объектам, автоматизацией обработки документов УПД и мобильным доступом через Telegram-бота.

### Технологический стек
- **Backend**: Python 3.11 + FastAPI (асинхронный)
- **База данных**: PostgreSQL 15 + Redis 7
- **Хранилище файлов**: MinIO/S3 (для XML УПД)
- **Telegram Bot**: aiogram (асинхронный)
- **Frontend**: React 18 + TypeScript + Vite (НЕ учитывается в backend-задачах)

### Ключевые процессы
1. **Учет рабочего времени бригад (РТБ)**: табели, расчет ФОТ
2. **Аренда техники и инструмента**: заявки, учет часов работы
3. **Заявки на материалы**: согласование, распределение УПД
4. **Обработка УПД**: парсинг XML, распределение по объектам/заявкам
5. **Поставки материалов (Deliveries)**: учет фактических поставок, сопоставление с заявками
6. **Excel экспорт**: шаблоны табелей, аналитические отчеты с форматированием

### Роли пользователей и их функции
- **FOREMAN** (Бригадир): создание заявок (материалы/техника), подача табелей РТБ
- **ACCOUNTANT** (Бухгалтер): загрузка УПД, просмотр отчетов, экспорт данных
- **HR_MANAGER**: проверка/утверждение табелей РТБ, указание ставок
- **EQUIPMENT_MANAGER**: утверждение заявок на технику, указание стоимости
- **MATERIALS_MANAGER**: обработка заявок на материалы, распределение УПД
- **PROCUREMENT_MANAGER**: согласование закупок материалов
- **MANAGER**: полный доступ, аналитика, назначение ролей
- **ADMIN**: техническое управление системой

---

## BACKEND: СТРУКТУРА И ПРАВИЛА

### Статусы и переходы

#### Табели РТБ (time_sheets)
- `ЧЕРНОВИК` → `НА РАССМОТРЕНИИ` → `ОТКОРРЕКТИРОВАННЫЙ` → `УТВЕРЖДЁННЫЙ`

#### Заявки на технику (equipment_orders)
- `НОВАЯ` → `УТВЕРЖДЕНА` → `В РАБОТЕ` → `ЗАВЕРШЕНА`
- Отмена: `ОТМЕНА ЗАПРОШЕНА` → `ОТМЕНЕНА`

#### Заявки на материалы (material_requests)
- `НОВАЯ` → `НА СОГЛАСОВАНИИ` → `В ОБРАБОТКЕ` → `ЗАКАЗАНО` → `ЧАСТИЧНО ПОСТАВЛЕНО` → `ОТГРУЖЕНО` → `ВЫПОЛНЕНА`

#### УПД (material_costs)
- `NEW` → `DISTRIBUTED` → `ARCHIVED`

#### Поставки (deliveries)
- `НОВАЯ` → `ПРИНЯТА` → `ЧАСТИЧНО_ОТГРУЖЕНА` → `ОТГРУЖЕНА` → `ЗАВЕРШЕНА`
- Отмена: `ОТМЕНЕНА`

### Основные таблицы БД
```sql
users (id, username, phone, role[], telegram_chat_id)
cost_objects (id, name, code, contract_number, contract_amount)
brigades (id, foreman_id, name)
time_sheets (id, brigade_id, period_start, period_end, status)
time_sheet_items (id, time_sheet_id, member_id, date, cost_object_id, hours)
equipment_orders (id, cost_object_id, foreman_id, equipment_type, status)
equipment_costs (id, equipment_order_id, hours_worked, hour_rate, total_amount)
material_requests (id, cost_object_id, foreman_id, status, urgency)
material_request_items (id, request_id, material_name, quantity, unit)
material_costs (id, cost_object_id, supplier_name, document_number, total_amount)
material_cost_items (id, material_cost_id, product_name, quantity, price, amount)
upd_distribution (id, material_cost_id, material_request_id, distributed_quantity)
deliveries (id, material_request_id, cost_object_id, supplier_name, delivery_date, status, total_amount, notes)
cost_entries (id, type, cost_object_id, date, amount) -- общая таблица затрат
```

### Модульная структура backend
```
auth/           - аутентификация, JWT, refresh-токены, RBAC
users/          - профили, роли, уведомления
objects/        - объекты учета
time_sheets/    - табели РТБ
equipment/      - заявки на технику и расчеты
materials/      - заявки на материалы
upd/            - загрузка, парсинг, распределение УПД
deliveries/     - поставки материалов (учет фактических поставок)
analytics/      - отчеты и аналитика, Excel экспорт
notifications/  - уведомления и Telegram-доставка
integrations/   - внешние интеграции
```

### Жизненный цикл УПД
1. **Загрузка XML**: файл сохраняется в S3/MinIO
2. **Парсинг**: извлечение поставщика, номера, даты, строк товаров, сумм
3. **Валидация**: проверка критичных полей (номер, дата, суммы, строки)
4. **Сохранение**: документ создается в БД со статусом `NEW`
5. **Распределение**: строки связываются с заявками/объектами
6. **Создание затрат**: запись в `material_costs` и `material_cost_items`
7. **Завершение**: УПД переводится в `DISTRIBUTED`/`ARCHIVED`

### API контракты (backend-only)

#### POST /api/v1/material-costs/upload
- **Вход**: XML-файл, метаданные (опционально)
- **Выход**: id УПД, статус, распарсенные поля

#### GET /api/v1/material-costs/unprocessed
- **Выход**: список УПД со статусом `NEW`

#### POST /api/v1/material-costs/{id}/distribute
- **Вход**: массив распределений по строкам (объект/заявка/кол-во/сумма)
- **Выход**: новый статус и агрегированные суммы

#### GET /api/v1/objects/{id}/costs
- **Вход**: период, типы затрат
- **Выход**: агрегированные суммы по объекту

#### Другие важные эндпоинты

**Табели РТБ:**
- `GET /api/v1/time-sheets` - список табелей (фильтры: статус, период)
- `POST /api/v1/time-sheets` - создать табель (из Excel/бота)
- `POST /api/v1/time-sheets/{id}/submit` - отправить на проверку
- `POST /api/v1/time-sheets/{id}/approve` - утвердить табель
- `PUT /api/v1/time-sheets/{id}/rate` - указать ставку за час

**Аренда техники:**
- `GET /api/v1/equipment-orders` - список заявок
- `POST /api/v1/equipment-orders` - создать заявку
- `POST /api/v1/equipment-orders/{id}/approve` - утвердить
- `PUT /api/v1/equipment-orders/{id}/cost` - указать стоимость
- `POST /api/v1/equipment-orders/{id}/hours` - учесть часы работы

**Заявки на материалы:**
- `GET /api/v1/material-requests` - список заявок
- `POST /api/v1/material-requests` - создать заявку
- `POST /api/v1/material-requests/{id}/approve` - согласовать
- `POST /api/v1/material-requests/{id}/process` - взять в обработку

**Поставки материалов:**
- `GET /api/v1/deliveries` - список поставок
- `POST /api/v1/deliveries` - создать поставку
- `GET /api/v1/deliveries/{id}` - получить поставку
- `PUT /api/v1/deliveries/{id}` - обновить поставку

**Excel функции:**
- `GET /api/v1/time-sheets/template` - скачать шаблон табеля Excel
- `GET /api/v1/analytics/export` - экспорт аналитики (3 листа: затраты по объектам, табели РТБ, техника)

---

## ПАРСЕР УПД: ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА

### Технические требования
- **Кодировка**: `windows-1251` обязательна
- **Версии формата**: 5.01 и 5.03
- **Поддерживаемые генераторы**:
  - Elewise LegalDoc 2.1
  - 1С:Бухгалтерия
  - Diadoc 1.0
  - VO2_xslt
- **НДС**: 0%, 20%, 22%
- **Строки**: минимум 1 строка
- **Единицы измерения**: словарь ОКЕИ + fallback на код

### Fallback-механизмы
- НДС: `НалСт` → `НДС` → 20% (дефолт)
- Номер документа: `НомерСчФ` → `НомерДок`
- Дата документа: `ДатаСчФ` → `ДатаДок`
- Услуги без количества: `qty=1`, `unit='услуга'`, `price=sum`

### Система трекинга проблем
Парсер обязан фиксировать проблемы в структуре `issues`:
```python
{
  "severity": "info" | "warning" | "error",
  "element": "элемент/атрибут",
  "message": "описание проблемы",
  "generator": "источник (если определен)",
  "value": "найденное значение (если нужно)"
}
```

**Назначение**:
- Логировать нестандартные случаи БЕЗ падения сервиса
- Собирать материал для расширения поддержки новых генераторов

---

## КРИТИЧЕСКИ ВАЖНЫЕ ЭЛЕМЕНТЫ КОДА

### 1. Парсер УПД (xml)
- `UPDParser.parse()` - точка входа, обязателен try/except
- `_detect_generator()` - определяет генератор и стратегию
- `_extract_supplier()` - извлекает имя/ИНН, учитывает формат 1С
- `_extract_items()` - выбор парсера строк и fallback
- `_parse_sved_tov()` - парсинг стандартных товаров
- `_parse_sved_tov_usl_sch()` - парсинг услуг без КолТов/ЦенаТов
- `convert_okei_to_unit()` - маппинг ОКЕИ → единицы измерения
- `OKEI_UNITS` - словарь кодов единиц измерения
- `ParsingIssue` + `parsing_issues` - фиксация проблем

### 2. API слой
- **Контроллеры УПД**: upload, distribute
- **Валидация**: Pydantic схемы для всех endpoint-ов
- **RBAC middleware**: контроль доступа по ролям
- **Аудит-лог**: запись изменений критичных сущностей

### 3. Бизнес-логика распределения
- Матрица распределения: один товар → несколько заявок/объектов
- Валидация сумм: пропорциональный расчет
- Статусы УПД: `NEW` → `DISTRIBUTED` → `ARCHIVED`

### 4. Затраты и агрегаты
- Создание `material_costs` и `material_cost_items`
- Единая модель `cost_entries` для отчетности
- Индексы по объектам и датам

### 5. Telegram Bot интеграция
- Webhook обработчик событий
- Состояния FSM для ключевых процессов (табели, заявки)
- Уведомления по ролям через Telegram

### 6. Безопасность и устойчивость
- Ограничение размера XML, защита от вредоносных payload
- Контроль кодировки `windows-1251`
- Централизованное логирование и метрики
- Резервные механизмы (fallback) для НДС, номера, даты

---

## ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### 1. УПД-услуги без КолТов/ЦенаТов
**Проблема**: УПД с услугами приходили без `КолТов`/`ЦенаТов`; парсер создавал строку с 0.  
**Решение**: Если `КолТов=0` и `ЦенаТов=0` при наличии суммы → трактовать как услугу: `qty=1`, `unit='услуга'`, `price=sum`.

### 2. Некорректный порядок fallback-парсинга
**Проблема**: Парсер возвращал пустую позицию, блокируя fallback.  
**Решение**: Возвращать `None` при отсутствии обязательных атрибутов, чтобы срабатывал fallback.

### 3. Формат названия поставщика (1С)
**Проблема**: 1С использует формат: `"ООО ... , ИНН/КПП ..."`; имя приходило с лишним хвостом.  
**Решение**: Обрезать часть после запятой и вытащить ИНН регулярным выражением.

### 4. НДС в разных атрибутах
**Проблема**: НДС может быть в `НалСт` или `НДС`; часть файлов не парсилась.  
**Решение**: Добавлен fallback: `НалСт` → `НДС` → `20%`.

### 5. Отсутствующий код ОКЕИ 356
**Проблема**: Единица измерения "час" (код 356) отображалась как "356".  
**Решение**: Добавлен код 356 в словарь ОКЕИ как "ч".

### 6. Разные генераторы УПД
**Проблема**: 4 разных генератора использовали разные структуры.  
**Решение**: Введена детекция генератора и гибкая логика извлечения полей.

### 7. Неизвестные будущие генераторы
**Проблема**: риск появления новых форматов.  
**Решение**: Система трекинга проблем — не падать и собирать диагностику.

### 8. Кодировка вывода и символы
**Проблема**: консоль Windows не печатает спецсимволы (✓, ⚠).  
**Решение**: тесты и отчеты используют ASCII-символы в логах.

### 9. Модуль deliveries с некорректными импортами (КРИТИЧНО)
**Проблема**: Модуль deliveries был создан с импортом из `app.auth.service`, которого не существует. Сервер не запускается.  
**Решение**: Использовать `from app.auth.dependencies import get_current_user`. Все async функции должны использовать `AsyncSession` и `await` для обращений к БД.  
**Статус**: ⚠️ ТРЕБУЕТ ПРОВЕРКИ - модуль может блокировать запуск сервера

---

## ПРОЦЕСС ВЫПОЛНЕНИЯ ЗАДАЧ

### Принципы
- **БЕЗ изменения UI** (внешний вид не учитывается)
- Изменения только в: backend, БД, парсинге, API, бот-интеграции
- Каждая задача имеет: цель, входные/выходные данные, критерии приемки

### Порядок работы

#### 1. Анализ требования
- Выделить бизнес-цель и модуль (`auth`/`objects`/`time_sheets`/`equipment`/`materials`/`upd`/`analytics`)
- Определить сущности БД и API эндпоинты

#### 2. Проектирование
- Схемы БД: таблицы/поля/связи/индексы
- Контракты API: вход/выход, статусы, ошибки
- Бизнес-логика: статусы, переходы, валидаторы

#### 3. Реализация
- Миграции БД
- Модели/репозитории/сервисы
- Контроллеры API
- Интеграции (Telegram, S3, очереди) при необходимости

#### 4. Проверки
- Юнит-тесты бизнес-логики
- Интеграционные тесты ключевых сценариев
- Регрессия парсинга УПД на наборе реальных файлов

#### 5. Наблюдаемость
- Логи ошибок и проблем парсинга
- Метрики по времени парсинга и количеству ошибок

#### 6. Документация
- Обновить `Система.md` по итогам
- Зафиксировать проблемы и решения (раздел 16.12)

### Обязательные артефакты каждой задачи
- Описание задачи (цель, результат, ограничения)
- Список измененных таблиц БД
- Список измененных API эндпоинтов
- Сценарии тестирования и фактический результат

### Специфично для УПД
Любые изменения парсера сопровождаются обновлением:
- Правил парсинга (16.11.5)
- Трекинга проблем (16.11.6)
- Списка проблем и решений (16.12)
- **Обязательный прогон на наборе реальных XML** (минимум 15 файлов)

### Критерии завершения задачи
✅ Все тесты проходят  
✅ Новые поля/статусы отражены в API контрактах  
✅ Нет регрессий по парсеру УПД  
✅ В документе `Система.md` отражены все изменения

---

## БЕЗОПАСНОСТЬ И АРХИТЕКТУРНЫЕ ТРЕБОВАНИЯ

### Аутентификация и авторизация
- JWT + refresh токены
- RBAC проверяется на каждом эндпоинте
- Санитизация входных XML, ограничение размера файлов
- Rate limit на upload и критичные операции

### Наблюдаемость
- **Логи**: все ошибки парсинга, распределения, статусов
- **Метрики**: количество УПД, среднее время парсинга, доля проблемных файлов
- **Алерты**: ошибки парсинга выше порога

### Миграции и версии API
- Все изменения БД — через миграции
- Все новые эндпоинты — через `/api/v1` с обратной совместимостью

### Ключевые индексы (обязательно создавать)
```sql
-- Для быстрого поиска по объектам
CREATE INDEX idx_cost_entries_object ON cost_entries(cost_object_id, date);
CREATE INDEX idx_material_costs_object ON material_costs(cost_object_id);
CREATE INDEX idx_equipment_orders_object ON equipment_orders(cost_object_id);

-- Для работы с табелями
CREATE INDEX idx_time_sheets_brigade ON time_sheets(brigade_id, period_start);
CREATE INDEX idx_time_sheet_items_date ON time_sheet_items(date, cost_object_id);

-- Для статусных запросов
CREATE INDEX idx_equipment_orders_status ON equipment_orders(status);
CREATE INDEX idx_material_requests_status ON material_requests(status);
CREATE INDEX idx_time_sheets_status ON time_sheets(status);
```

---

## ВАЖНО ПРИ ГЕНЕРАЦИИ КОДА

1. **Всегда используй асинхронный код** для FastAPI эндпоинтов
2. **Обязательно оборачивай парсинг УПД в try/except** с логированием проблем
3. **Используй Pydantic** для валидации входных/выходных данных
4. **Добавляй RBAC декораторы** ко всем endpoint-ам
5. **Создавай миграции** для любых изменений схемы БД
6. **Логируй все проблемы парсинга** в структуру `parsing_issues`
7. **Тестируй fallback-механизмы** на реальных XML файлах
8. **Документируй все изменения** в соответствующих .md файлах

### ⚠️ КРИТИЧНЫЕ ПАТТЕРНЫ ИМПОРТОВ

**ПРАВИЛЬНО (используй всегда):**
```python
from app.auth.dependencies import get_current_user
from app.models import User
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_db

@router.post("/")
async def create_item(
    data: ItemCreate,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    # Проверка ролей
    user_roles = [role.value for role in current_user.roles]
    if "REQUIRED_ROLE" not in user_roles:
        raise HTTPException(status_code=403, detail="Недостаточно прав")
    
    # Работа с БД (ОБЯЗАТЕЛЬНО await)
    service = SomeService(db)
    result = await service.create(data, current_user.id)
    return result
```

**НЕПРАВИЛЬНО (НЕ используй):**
```python
# ❌ НЕ СУЩЕСТВУЕТ
from app.auth.service import AuthService

# ❌ Синхронная Session вместо AsyncSession
from sqlalchemy.orm import Session

# ❌ Обращение к current_user как к словарю
current_user.get("roles")  # НЕПРАВИЛЬНО

# ❌ Вызов БД без await
result = service.create(data)  # НЕПРАВИЛЬНО
```

### Правильная структура сервиса:

```python
class SomeService:
    def __init__(self, db: AsyncSession):
        self.db = db
    
    async def create(self, data: SomeCreate, user_id: int):
        # Все запросы к БД - async
        result = await self.db.execute(...)
        await self.db.commit()
        return result
```

---

## СЛОВАРЬ ОКЕИ (МИНИМУМ)

```python
OKEI_UNITS = {
    "796": "шт",
    "006": "м",
    "055": "м²",
    "113": "м³",
    "166": "кг",
    "168": "т",
    "356": "ч",
    "112": "л",
    # ... расширяется по мере необходимости
}
```

Для неизвестных кодов: возвращать код как строку + логировать проблему.

---

## БАЗА ЗНАНИЙ ПРОЕКТА - НАВИГАЦИЯ ПО ДОКУМЕНТАЦИИ

### 📂 Структура документации

Вся документация проекта организована в **`docs/`** с категориями:

```
docs/
├── 01_CORE/       - Основные системные документы (ТЗ, архитектура, БД)
├── 02_GUIDES/     - Инструкции по запуску, тестированию, быстрым решениям
├── 03_REPORTS/    - Отчеты о выполненной разработке
├── 04_STATUS/     - Текущий статус проекта и версионирование
├── 05_TELEGRAM/   - Документация по Telegram-боту
└── 06_TECHNICAL/  - Техническая документация (архитектура, парсер)
```

### 🎯 Главный навигатор

**KNOWLEDGE_BASE_INDEX.md** - полный индекс всей документации с:
- Описанием каждого файла и его назначения
- Правилами "когда использовать"
- Индексом по ключевым словам
- Алгоритмом поиска информации для AI

### ⚡ Быстрая навигация (ТОП-5 документов)

#### 1. **docs/01_CORE/Система.md** ⭐
**Главный технический документ**
Используй когда нужно:
- Понять бизнес-логику (статусы, роли, процессы)
- Схемы БД (таблицы, связи, индексы)
- API контракты (endpoints, входы/выходы)
- Правила парсинга УПД (форматы, fallback, ОКЕИ)
- Известные проблемы и решения

#### 2. **docs/03_REPORTS/REPORT_FINAL.md**
**Что сделано в проекте**
Используй когда нужно:
- Понять какие функции реализованы
- Проверить статус модуля
- Посмотреть статистику разработки

#### 3. **docs/04_STATUS/STATUS_27_JAN_2026.md**
**Текущий статус проекта**
Используй когда нужно:
- Узнать текущее состояние
- Проверить работающие компоненты
- Список известных багов

#### 4. **docs/02_GUIDES/ЗАПУСК_СЕРВЕРА.md**
**Запуск и отладка**
Используй когда:
- Проблемы с запуском backend
- Ошибки сервера
- Нужны команды запуска

#### 5. **docs/05_TELEGRAM/TELEGRAM_BOT_SETUP.md**
**Настройка Telegram-бота**
Используй когда:
- Первая настройка бота
- Проблемы с подключением
- Конфигурация webhook/polling

### 🔍 Быстрый поиск по теме

| Тема | Документ | Раздел |
|------|----------|--------|
| Аутентификация, JWT | docs/01_CORE/Система.md | Раздел 8 "Auth" |
| БД, миграции, индексы | docs/01_CORE/Система.md | Раздел 6 "БД" |
| API endpoints | docs/01_CORE/Система.md | Раздел 7 "API" |
| Табели РТБ | docs/01_CORE/Система.md | Раздел 4.1 |
| Аренда техники | docs/01_CORE/Система.md | Раздел 4.2 |
| Материалы и УПД | docs/01_CORE/Система.md | Раздел 4.3 |
| Парсинг УПД, XML | docs/01_CORE/Система.md + docs/06_TECHNICAL/PARSER_ROBUSTNESS.md | Раздел 16.11 |
| Роли и права | docs/01_CORE/Система.md | Раздел 3 |
| Статусы сущностей | docs/01_CORE/Система.md | Раздел 4 |
| Баги и решения | docs/02_GUIDES/QUICK_FIX_CHEATSHEET.md | - |
| Поставки (deliveries) | docs/03_REPORTS/REPORT_DELIVERIES_IMPLEMENTATION.md | - |
| Excel экспорт | docs/03_REPORTS/REPORT_EXCEL_FUNCTIONS.md | - |

### 📋 Алгоритм работы с документацией

**ЭТАП 1: Определи тип вопроса**
```
Вопрос о:
├── Архитектуре/БД/API/Бизнес-логике → docs/01_CORE/Система.md
├── Запуске/Установке/Ошибках → docs/02_GUIDES/
├── Что сделано/Реализация → docs/03_REPORTS/REPORT_FINAL.md
├── Текущий статус → docs/04_STATUS/STATUS_27_JAN_2026.md
├── Telegram-боте → docs/05_TELEGRAM/
└── Технических деталях → docs/06_TECHNICAL/
```

**ЭТАП 2: Читай целенаправленно**
1. НЕ читай весь документ - используй семантический поиск
2. Найди релевантный раздел по индексу
3. Читай только нужную секцию
4. Если недостаточно - расширь диапазон

**ЭТАП 3: Применяй информацию**
1. Используй найденное напрямую
2. Ссылайся на документы вместо пересказа
3. При неясности - уточни у пользователя

### ⚠️ Правила работы с базой знаний

✅ **ДЕЛАЙ:**
- Используй KNOWLEDGE_BASE_INDEX.md как точку входа
- Ищи по ключевым словам через индекс
- Читай только релевантные разделы
- Ссылайся на документы вместо дублирования
- Обновляй индекс при добавлении новых файлов

❌ **НЕ ДЕЛАЙ:**
- Не читай все документы подряд
- Не дублируй информацию из документов
- Не игнорируй структуру папок
- Не создавай новые .md файлы без запроса
- Не перезаписывай документы без разрешения

### 📖 Полная справка

Для полной информации о всех документах, их назначении и правилах использования:
→ Читай **KNOWLEDGE_BASE_INDEX.md** в корне проекта