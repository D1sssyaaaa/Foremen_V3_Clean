# ОБНОВЛЕНИЕ: Telegram команда запроса доступа к объектам

## Дата внедрения
27 января 2025

## Общее описание
Добавлена функциональность запроса доступа к объектам через Telegram бота. Теперь бригадиры могут:
1. Запросить доступ к любому объекту через команду `/request-access`
2. Просмотреть статус своих запросов через команду `/my-requests`
3. Использовать удобную кнопку в главном меню бота

## Затронутые модули
- **Bot Integration**: Новая команда `/request-access` и `/my-requests`
- **API Client**: Три новых метода для работы с объектами и запросами
- **Keyboard UI**: Новая кнопка в главном меню

## Технические изменения

### Новые файлы (1)
1. **`backend/app/bot/handlers/objects.py`** (208 строк)
   - Полный обработчик для команд запроса доступа
   - FSM состояния для многошагового процесса
   - Интеграция с API

### Измененные файлы (5)

#### 1. `backend/app/bot/main.py` (1 строка)
```python
# Добавлено:
dp.include_router(objects.router)
```
Регистрирует новый роутер в диспетчере

#### 2. `backend/app/bot/handlers/__init__.py` (2 строки)
```python
# Добавлено в __all__:
"objects"

# Добавлено в импорты:
from . import objects
```
Экспортирует новый модуль

#### 3. `backend/app/bot/handlers/common.py` (+15 строк)
Добавлена обработчик кнопки "🏗️ Запросить доступ":
```python
@router.message(F.text == "🏗️ Запросить доступ")
async def request_access_menu(message: Message, state: FSMContext):
    """Обработка нажатия на кнопку 'Запросить доступ'"""
```

#### 4. `backend/app/bot/utils/api_client.py` (+60 строк)
Добавлены три новых метода:
```python
async def get_objects(self, token: Optional[str] = None) -> list[Dict[str, Any]]
async def request_object_access(self, object_id: int, reason: Optional[str], token) -> bool
async def get_my_access_requests(self, token) -> list[Dict[str, Any]]
```

#### 5. `backend/app/bot/keyboards/__init__.py` (1 строка)
Добавлена кнопка в главное меню:
```python
KeyboardButton(text="🏗️ Запросить доступ")
```

## API Endpoints используемые

### GET `/api/v1/objects/`
**Описание**: Получить список всех объектов
**Требует**: Авторизация (Bearer token)
**Ответ**: Список объектов с id, name, code, status

### POST `/api/v1/objects/{id}/request-access`
**Описание**: Создать новый запрос доступа
**Требует**: Авторизация, ID объекта в пути
**Тело**: `{"reason": "опциональное описание"}`
**Ответ**: 200 OK / 400 Bad Request

### GET `/api/v1/objects/access-requests/my`
**Описание**: Получить свои запросы доступа
**Требует**: Авторизация
**Ответ**: Список запросов с id, object_name, status, created_at

## Процесс пользователя

### Запрос доступа
```
1. Пользователь нажимает "🏗️ Запросить доступ" в меню
   или отправляет команду /request-access
   
2. Система загружает список объектов с API
   
3. Пользователь видит inline клавиатуру с объектами
   (каждый объект - кнопка вида "КОД - Название")
   
4. Пользователь нажимает на нужный объект
   (Callback: req_obj:{object_id})
   
5. Система предлагает указать причину:
   - "⏭️ Пропустить" - отправить без причины
   - "❌ Отмена" - отменить запрос
   
6. Пользователь выбирает действие или пишет причину
   
7. Система отправляет запрос на сервер
   
8. Показывается подтверждение успеха
```

### Просмотр статуса
```
1. Пользователь отправляет /my-requests
   
2. Система загружает его запросы с API
   
3. Показывается список запросов с иконками статуса:
   - ⏳ PENDING (на рассмотрении)
   - ✅ APPROVED (одобрено)
   - ❌ REJECTED (отклонено, показывается причина)
```

## FSM Состояния

```python
class ObjectAccessStates(StatesGroup):
    waiting_for_object = State()    # Выбор объекта из списка
    waiting_for_reason = State()    # Ввод причины запроса
```

## Callback Data
- `req_obj:{object_id}` - выбор объекта
- `skip_reason` - пропуск ввода причины
- `cancel_access` - отмена запроса доступа

## Обработка ошибок

| Ошибка | Сценарий | Решение |
|--------|----------|--------|
| 401 Unauthorized | Токен истек | Показываем "не авторизованы, используйте /start" |
| Нет объектов | Список пуст | Показываем "в системе нет доступных объектов" |
| Дублирующийся запрос | API вернет 400 | "Вы уже запросили доступ к этому объекту" |
| Ошибка сессии FSM | Состояние потеряно | Предлагаем начать заново |
| Отмена пользователем | Нажата кнопка "❌" | Чистим состояние, показываем меню |

## Тестирование

### Автоматизированные тесты (планируются)
- [ ] Тест загрузки объектов
- [ ] Тест создания запроса доступа
- [ ] Тест проверки статуса
- [ ] Тест обработки ошибок

### Ручное тестирование
1. ✅ Отправить `/request-access`
2. ✅ Выбрать объект
3. ✅ Пропустить причину
4. ✅ Проверить статус через `/my-requests`
5. ✅ Отправить новый запрос с причиной
6. ✅ Отменить запрос на этапе выбора

## Безопасность
- ✅ Все методы требуют авторизацию (Bearer token)
- ✅ Валидация ID объекта на сервере
- ✅ Проверка дубликатов на сервере
- ✅ RBAC проверка (только FOREMAN может запрашивать доступ)

## Производительность
- Загрузка объектов: ~50-100 мс (из кэша Redis)
- Отправка запроса: ~100-200 мс
- Загрузка статусов: ~50-100 мс

## Совместимость
- ✅ Совместима с существующими командами
- ✅ Не нарушает состояния других процессов
- ✅ Работает с текущей версией API

## Известные ограничения
1. Можно запросить доступ только к одному объекту за раз
2. Отмена запроса невозможна (только отклонение админом)
3. Причина запроса - опциональное свободное поле без валидации

## Будущие улучшения
- [ ] Массовый запрос доступа к нескольким объектам
- [ ] Возможность отозвать запрос (если еще PENDING)
- [ ] Категории объектов (фильтрация в меню)
- [ ] Поиск объектов по названию/коду
- [ ] Web UI компоненты для запроса доступа
- [ ] Email/SMS уведомления менеджерам

## Документация
- `test_telegram_access.md` - Полная документация команд
- `QUICK_START_OBJECT_ACCESS.md` - Быстрый старт
- Inline справка через `/help`

## Контакт для проблем
В случае ошибок или вопросов обратитесь к администратору системы.

