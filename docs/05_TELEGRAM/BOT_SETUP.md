# Запуск Telegram Bot

## 1. Создание бота через @BotFather

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота, например: `Construction Costs Bot`
4. Введите username бота, например: `construction_costs_test_bot`
5. Скопируйте полученный токен

## 2. Настройка .env файла

Откройте файл `backend/.env` и добавьте токен бота:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
API_BASE_URL=http://localhost:8000/api/v1
```

## 3. Запуск бота

### Вариант 1: Запуск напрямую

```bash
cd backend
python -m app.bot.main
```

### Вариант 2: Запуск в фоне

```bash
cd backend
start python -m app.bot.main
```

## 4. Первый запуск

1. Найдите вашего бота в Telegram по username
2. Отправьте команду `/start`
3. Вы увидите главное меню с кнопками

## 5. Доступные функции

### ✅ Реализовано:
- **📦 Заявка на материалы**
  - Обычные материалы (кирпич, цемент, гвозди и т.д.)
  - Инертные материалы (песок, щебень, ПГС, раствор) с обязательным временем доставки
  - Выбор срочности (обычная, срочная, критичная)
  - Добавление нескольких позиций

- **🚜 Заявка на технику**
  - Выбор типа техники (экскаватор, кран, бульдозер, погрузчик, бетономешалка)
  - Указание даты начала и длительности аренды
  - Описание работ

- **📊 Подать табель РТБ**
  - Загрузка Excel файла с табелем
  - Добавление комментария
  - Отправка на проверку HR-менеджеру

### ⏳ В разработке:
- **📈 Мои заявки** - просмотр всех заявок и их статусов
- **🔔 Уведомления** - автоматические уведомления об изменении статусов

## 6. Формат Excel для табеля РТБ

Табель должен содержать следующие колонки:

| ФИО | Дата | Объект | Часы |
|-----|------|--------|------|
| Иванов И.И. | 20.01.2026 | 1 | 8 |
| Петров П.П. | 20.01.2026 | 1 | 8 |
| Иванов И.И. | 21.01.2026 | 2 | 6 |

- **ФИО** - полное имя работника
- **Дата** - дата работы в формате ДД.ММ.ГГГГ
- **Объект** - ID объекта учета
- **Часы** - количество отработанных часов

## 7. Особенности инертных материалов

При создании заявки на **инертные материалы** (песок, щебень, ПГС, раствор):
- 🔴 **ОБЯЗАТЕЛЬНО** указать желаемое время доставки
- Примеры формата времени:
  - `08:00-12:00` - конкретный диапазон
  - `14:00-18:00` - вечерняя доставка
  - `утро`, `день`, `вечер` - общее время
  - `с 9 до 17` - произвольный формат

## 8. Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/cancel` - Отменить текущее действие

## 9. Проверка работы

Откройте два терминала:

**Терминал 1 - Backend API:**
```bash
cd backend
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

**Терминал 2 - Telegram Bot:**
```bash
cd backend
python -m app.bot.main
```

В консоли должно появиться:
```
🤖 Construction Costs Bot started
📡 API Base URL: http://localhost:8000/api/v1
```

## 10. Решение проблем

### Бот не отвечает
- Проверьте, что бот запущен (смотрите логи в консоли)
- Проверьте токен в `.env` файле
- Убедитесь, что backend API работает

### Ошибка при создании заявки
- Убедитесь, что backend API запущен на порту 8000
- Проверьте, что пользователь авторизован в системе
- Объект с указанным ID должен существовать в БД

### Excel файл не загружается
- Проверьте формат файла (.xlsx или .xls)
- Размер файла не должен превышать 20 MB
- Первая строка должна содержать заголовки

## 11. Логи и отладка

Бот пишет логи в консоль. Уровни логирования:
- `INFO` - информационные сообщения
- `WARNING` - предупреждения
- `ERROR` - ошибки

Для детальной отладки измените уровень в `app/bot/main.py`:
```python
logging.basicConfig(level=logging.DEBUG)
```
