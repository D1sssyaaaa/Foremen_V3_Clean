# Отчёт: Улучшения обработки УПД

**Дата:** 26 января 2026 г.  
**Модуль:** УПД — обнаружение дубликатов, история распределения, WebSocket уведомления

---

## ✅ Что сделано

### 1. Обнаружение дубликатов УПД

**Новый статус DUPLICATE:**
```python
class UPDStatus(str, PyEnum):
    NEW = "NEW"
    DISTRIBUTED = "DISTRIBUTED"
    ARCHIVED = "ARCHIVED"
    DUPLICATE = "DUPLICATE"  # ← Новый статус
```

**Новые поля в MaterialCost:**
```python
duplicate_of_id = Column(Integer, ForeignKey("material_costs.id"), nullable=True)
duplicate_checked_at = Column(DateTime, nullable=True)
duplicate_of = relationship("MaterialCost", remote_side=[id])
```

**Применение:**
- Автоматическая проверка при загрузке УПД
- Сравнение по номеру документа + дата + ИНН поставщика
- Дубликат помечается статусом DUPLICATE и ссылается на оригинал
- Бухгалтер получает WebSocket уведомление о дубликате

---

### 2. Методы UPDService для работы с дубликатами

#### check_duplicate()
Проверка УПД на дубликат

**Входные данные:**
- `document_number` — номер документа
- `document_date` — дата документа
- `supplier_inn` — ИНН поставщика (опционально)

**Логика:**
1. Поиск в БД УПД с таким же номером и датой
2. Если указан ИНН — дополнительная фильтрация
3. Исключаются сами дубликаты (status != DUPLICATE)

**Возвращает:** MaterialCost если найден оригинал, иначе None

---

#### mark_as_duplicate()
Пометить УПД как дубликат вручную

**Входные данные:**
- `upd_id` — ID УПД, который является дубликатом
- `original_upd_id` — ID оригинального УПД

**Логика:**
1. Проверка существования обоих УПД
2. Установка статуса DUPLICATE
3. Установка ссылки на оригинал
4. Фиксация времени проверки

**Возвращает:** обновлённый MaterialCost

**Исключения:**
- `ValueError` — если УПД не найден

---

#### find_potential_duplicates()
Поиск потенциальных дубликатов

**Входные данные:**
- `upd_id` — ID УПД для проверки
- `tolerance_days` — допустимое отклонение по дате (по умолчанию 3 дня)

**Логика:**
1. Получение УПД для проверки
2. Формирование диапазона дат (±tolerance_days)
3. Поиск УПД с таким же номером в этом диапазоне
4. Если есть ИНН — дополнительная фильтрация
5. Исключение самого УПД и дубликатов

**Возвращает:** список потенциальных дубликатов

**Применение:**
- Перед загрузкой — предупредить бухгалтера
- После загрузки — показать похожие документы для проверки

---

### 3. Обновлённый метод upload_upd

**Новый параметр:**
```python
async def upload_upd(
    xml_content: bytes, 
    file_path: str,
    auto_check_duplicate: bool = True  # ← Автопроверка
) -> MaterialCost
```

**Изменения в логике:**
1. Парсинг XML
2. **Автопроверка на дубликаты** (если `auto_check_duplicate=True`)
3. Если дубликат найден:
   - Статус устанавливается в DUPLICATE
   - Заполняется `duplicate_of_id`
   - Фиксируется `duplicate_checked_at`
4. Сохранение в БД
5. Создание строк товаров

**Результат:**
- УПД создаётся в любом случае (для анализа)
- Но дубликаты сразу помечаются и не попадают в обработку

---

## 📡 Новые API endpoints

### GET /api/v1/material-costs/{upd_id}/duplicates

Поиск потенциальных дубликатов УПД

**Доступ:** ACCOUNTANT, MATERIALS_MANAGER, MANAGER

**Параметры:**
- `tolerance_days` (query, optional) — отклонение по дате (по умолчанию 3)

**Запрос:**
```bash
GET /api/v1/material-costs/15/duplicates?tolerance_days=5
Authorization: Bearer <token>
```

**Ответ:**
```json
[
  {
    "id": 12,
    "document_number": "УПД-2026-001",
    "document_date": "2026-01-20",
    "supplier_name": "ООО Поставщик",
    "total_with_vat": 150000.00,
    "items_count": 5,
    "status": "NEW",
    "created_at": "2026-01-20T10:00:00"
  }
]
```

**Применение:**
- Проверка перед обработкой УПД
- Поиск ранее загруженных документов
- Анализ возможных ошибок

---

### POST /api/v1/material-costs/{upd_id}/mark-duplicate

Пометить УПД как дубликат

**Доступ:** ACCOUNTANT, MANAGER

**Параметры:**
- `original_upd_id` (query) — ID оригинального УПД

**Запрос:**
```bash
POST /api/v1/material-costs/15/mark-duplicate?original_upd_id=12
Authorization: Bearer <token>
```

**Ответ:**
```json
{
  "message": "УПД #15 помечен как дубликат УПД #12",
  "upd_id": 15,
  "status": "DUPLICATE",
  "duplicate_of_id": 12,
  "duplicate_checked_at": "2026-01-26T22:50:00"
}
```

**Ошибки:**
- `400` — УПД не найден или оригинал не существует
- `404` — УПД с указанным ID не найден

**Интеграция:**
- ✅ Логируется в `audit_log`
- ✅ WebSocket уведомление менеджеру

---

## 🔔 WebSocket уведомления для УПД

### При загрузке нового УПД

**Событие:** `upd_uploaded`

**Получатели:** ACCOUNTANT, MATERIALS_MANAGER

**Сообщение:**
```json
{
  "type": "notification",
  "notification_type": "upd_uploaded",
  "title": "Новый УПД загружен",
  "message": "УПД №УПД-2026-001 от 2026-01-26, поставщик: ООО Поставщик, сумма: 150000.00 ₽",
  "data": {
    "upd_id": 15,
    "document_number": "УПД-2026-001",
    "supplier_name": "ООО Поставщик",
    "total_amount": 150000.00
  },
  "timestamp": "2026-01-26T22:50:00"
}
```

**Применение:**
- Немедленное оповещение о новом документе
- Менеджер по снабжению может сразу начать обработку
- Бухгалтер видит новые поступления в реальном времени

---

### При обнаружении дубликата

**Событие:** `upd_duplicate_detected`

**Получатели:** ACCOUNTANT, MATERIALS_MANAGER

**Сообщение:**
```json
{
  "type": "notification",
  "notification_type": "upd_duplicate_detected",
  "title": "Обнаружен дубликат УПД",
  "message": "УПД №УПД-2026-001 от 2026-01-26 является дубликатом УПД #12",
  "data": {
    "upd_id": 15,
    "document_number": "УПД-2026-001",
    "duplicate_of_id": 12,
    "supplier_name": "ООО Поставщик"
  },
  "timestamp": "2026-01-26T22:50:00"
}
```

**Применение:**
- Предотвращение двойного учёта
- Немедленное оповещение бухгалтера
- Возможность проверить оба документа

---

### При распределении УПД

**Событие:** `upd_distributed`

**Получатели:** ACCOUNTANT, MANAGER

**Сообщение:**
```json
{
  "type": "notification",
  "notification_type": "upd_distributed",
  "title": "УПД распределён",
  "message": "УПД №УПД-2026-001 распределён на 3 объектов/заявок на сумму 150000.00 ₽",
  "data": {
    "upd_id": 15,
    "document_number": "УПД-2026-001",
    "total_distributed": 150000.00,
    "distributions_count": 3
  },
  "timestamp": "2026-01-26T22:50:00"
}
```

**Применение:**
- Оповещение о завершении обработки
- Руководитель видит распределение затрат
- Бухгалтер может приступить к закрытию документа

---

## 🔄 Обновлённый жизненный цикл УПД

### Стандартный поток:
```
1. Загрузка XML → Парсинг
2. Проверка на дубликаты
3. Если дубликат:
   ├─ Статус: DUPLICATE
   ├─ WebSocket: upd_duplicate_detected
   └─ Сохранение (для анализа)
   
4. Если новый:
   ├─ Статус: NEW
   ├─ WebSocket: upd_uploaded
   └─ Ожидание распределения
   
5. Распределение:
   ├─ Создание записей затрат
   ├─ Статус: DISTRIBUTED
   └─ WebSocket: upd_distributed
   
6. Архивация объекта:
   └─ Статус всех УПД: ARCHIVED
```

### Проверка дубликатов:
```
Критерии совпадения:
- document_number (обязательно)
- document_date (обязательно)
- supplier_inn (если указан)

Допуск по дате: ±3 дня (настраивается)
```

---

## 📊 Обновлённая база данных

**Количество таблиц:** 19 (без изменений)

**Изменённая таблица:**
- `material_costs` — добавлены поля:
  - `duplicate_of_id` — ссылка на оригинальный УПД
  - `duplicate_checked_at` — когда проверялся на дубликаты

**Индексы:**
```sql
-- Для быстрого поиска дубликатов
CREATE INDEX idx_material_costs_document 
ON material_costs(document_number, document_date);

CREATE INDEX idx_material_costs_supplier_inn 
ON material_costs(supplier_inn);
```

---

## 🎯 Сценарии использования

### Сценарий 1: Автоматическое обнаружение дубликата

```bash
# 1. Бухгалтер загружает УПД
POST /api/v1/material-costs/upload
→ Файл: УПД-2026-001.xml
→ Парсинг успешен

# 2. Система автоматически проверяет дубликаты
→ Найден УПД с таким же номером и датой (ID=12)

# 3. Новый УПД сохраняется со статусом DUPLICATE
→ ID: 15
→ Status: DUPLICATE
→ duplicate_of_id: 12

# 4. WebSocket уведомление
→ Бухгалтер: "Обнаружен дубликат УПД"
→ В уведомлении: ссылка на оригинал #12

# 5. Бухгалтер проверяет оба документа
GET /api/v1/material-costs/12  # Оригинал
GET /api/v1/material-costs/15  # Дубликат

# 6. Бухгалтер решает оставить оригинал
# Дубликат остаётся в базе для истории, но не обрабатывается
```

---

### Сценарий 2: Ручная пометка как дубликат

```bash
# 1. Бухгалтер загрузил два УПД
POST /api/v1/material-costs/upload
→ УПД #18: УПД-2026-002 от 2026-01-25

POST /api/v1/material-costs/upload
→ УПД #19: УПД-2026-002А от 2026-01-26

# 2. Бухгалтер понимает, что это корректировка дубликата
# (немного другой номер, дата отличается на 1 день)

# 3. Поиск потенциальных дубликатов
GET /api/v1/material-costs/19/duplicates?tolerance_days=5
→ Найден УПД #18

# 4. Ручная пометка как дубликат
POST /api/v1/material-costs/19/mark-duplicate?original_upd_id=18
→ УПД #19 помечен как дубликат УПД #18

# 5. WebSocket уведомление менеджеру
→ "УПД #19 помечен как дубликат"
```

---

### Сценарий 3: Загрузка без автопроверки

```python
# Для тестирования или особых случаев
upd = await service.upload_upd(
    xml_content=content,
    file_path=path,
    auto_check_duplicate=False  # Отключение автопроверки
)

# Ручная проверка позже
duplicate = await service.check_duplicate(
    document_number=upd.document_number,
    document_date=upd.document_date,
    supplier_inn=upd.supplier_inn
)

if duplicate:
    await service.mark_as_duplicate(upd.id, duplicate.id)
```

---

## 🔒 Права доступа

| Действие | ACCOUNTANT | MATERIALS_MANAGER | MANAGER | ADMIN |
|----------|------------|-------------------|---------|-------|
| Загрузка УПД | ✅ | ✅ | ❌ | ✅ |
| Поиск дубликатов | ✅ | ✅ | ✅ | ✅ |
| Пометка как дубликат | ✅ | ❌ | ✅ | ✅ |
| Получение WebSocket | ✅ | ✅ | ✅ | ✅ |

---

## 🔗 Интеграции

### Audit Log
Все действия с дубликатами логируются:

**Автоматическое обнаружение:**
```json
{
  "action": "UPD_DUPLICATE_AUTO_DETECTED",
  "entity_type": "MaterialCost",
  "entity_id": 15,
  "old_value": null,
  "new_value": "DUPLICATE",
  "description": "Автоматически обнаружен дубликат УПД #15 для УПД #12"
}
```

**Ручная пометка:**
```json
{
  "action": "UPD_MARKED_AS_DUPLICATE",
  "entity_type": "MaterialCost",
  "entity_id": 19,
  "old_value": "NEW",
  "new_value": "DUPLICATE",
  "description": "УПД #19 помечен как дубликат УПД #18"
}
```

### WebSocket Manager
Использует существующую инфраструктуру:
- `ConnectionManager` для управления соединениями
- `broadcast_to_roles()` для уведомлений по ролям
- Автоматическая очистка закрытых соединений

---

## ✨ Итоги

**Добавлено:**
- ✅ Статус DUPLICATE для УПД
- ✅ Поля duplicate_of_id и duplicate_checked_at
- ✅ 3 новых метода в UPDService
  - check_duplicate()
  - mark_as_duplicate()
  - find_potential_duplicates()
- ✅ Автопроверка при загрузке УПД
- ✅ 2 новых API endpoint
  - GET /{upd_id}/duplicates
  - POST /{upd_id}/mark-duplicate
- ✅ 3 типа WebSocket уведомлений
  - upd_uploaded
  - upd_duplicate_detected
  - upd_distributed
- ✅ Интеграция с audit_log
- ✅ Обновлённый upload endpoint

**Файлы изменены:**
- `app/core/models_base.py` — UPDStatus.DUPLICATE
- `app/models.py` — поля duplicate_of_id, duplicate_checked_at
- `app/upd/service.py` — 3 новых метода + изменён upload_upd
- `app/upd/router.py` — 2 новых endpoint + WebSocket в upload/distribute
- База данных — 2 новых поля в material_costs

**Строк кода:** ~250 новых строк

---

## 🚀 Что дальше?

**Готово к реализации:**

1. **История распределения УПД** 📊
   - Таблица upd_distribution_history
   - Кто, когда, что изменил
   - API для получения истории

2. **Корректировка распределения** ✏️
   - Изменение уже обработанного УПД
   - Пересчёт сумм
   - Логирование изменений

3. **Заявки на материалы** 📦
   - Привязка УПД к заявкам
   - Частичная поставка
   - Автозакрытие заявок

**Сервер запущен:** ✅ http://127.0.0.1:8000  
**Swagger UI:** http://127.0.0.1:8000/docs  
**WebSocket:** ws://127.0.0.1:8000/api/v1/ws  
**БД:** 19 таблиц, готова к работе

---

## 📝 Примечания

**Технические детали:**
- Проверка дубликатов — O(1) по индексу (document_number, document_date)
- Толерантность по дате настраивается (по умолчанию ±3 дня)
- Дубликаты сохраняются для аудита и анализа
- WebSocket уведомления не блокируют основной поток

**Известные ограничения:**
- Проверка только по номеру + дате + ИНН
- Нет сравнения содержимого УПД (строк товаров)
- Автопроверка работает только при загрузке через API

**Будущие улучшения:**
- Сравнение по содержимому (количество строк, суммы)
- Настраиваемые правила обнаружения дубликатов
- Batch-проверка существующих УПД
- Экспорт списка дубликатов в Excel

---

## 🕒 История распределения УПД

### Новая модель UPDDistributionHistory

**Таблица:** `upd_distribution_history`

```python
class UPDDistributionHistory(Base, TimestampMixin):
    """История изменений распределения УПД"""
    material_cost_id = Column(Integer, FK("material_costs.id"))
    user_id = Column(Integer, FK("users.id"))
    action = Column(String(50))  # CREATE, UPDATE, DELETE
    old_distribution = Column(JSON)  # Старое распределение
    new_distribution = Column(JSON)  # Новое распределение
    description = Column(Text)  # Описание изменения
```

**Применение:**
- Отслеживание всех изменений распределения
- Кто и когда изменил
- Что было до и что стало
- Аудит изменений для бухгалтерии

---

### Метод log_distribution_history()

**Входные данные:**
- `material_cost_id` — ID УПД
- `user_id` — ID пользователя
- `action` — тип действия (CREATE, UPDATE, DELETE)
- `old_distribution` — старое распределение (JSON)
- `new_distribution` — новое распределение (JSON)
- `description` — описание изменения

**Логика:**
1. Создание записи UPDDistributionHistory
2. Сохранение JSON с распределениями
3. Фиксация времени изменения
4. Связь с пользователем

**Структура JSON распределения:**
```json
{
  "distributions": [
    {
      "material_cost_item_id": 5,
      "material_request_id": 10,
      "distributed_quantity": 100.0,
      "distributed_amount": 15000.0
    }
  ],
  "total_amount": 15000.0
}
```

---

### Метод get_distribution_history()

**Входные данные:**
- `upd_id` — ID УПД
- `limit` — максимальное количество записей (по умолчанию 50)

**Возвращает:** список записей истории в обратном хронологическом порядке

**Применение:**
- Просмотр всех изменений УПД
- Выявление ошибок в распределении
- Аудит для бухгалтерии

---

## ✏️ Корректировка распределения УПД

### Метод redistribute_upd()

**Входные данные:**
- `upd_id` — ID УПД
- `user_id` — ID пользователя
- `new_distributions` — новое распределение

**Логика:**
1. Проверка статуса УПД (нельзя ARCHIVED/DUPLICATE)
2. Валидация нового распределения
3. Сохранение старого распределения для истории
4. Удаление старых записей распределения
5. Удаление старых записей затрат (CostEntry)
6. Создание новых записей распределения
7. Создание новых записей затрат
8. Логирование в историю (action=UPDATE)
9. Commit и обновление УПД

**Возвращает:** обновлённый MaterialCost

**Исключения:**
- `ValueError` — если УПД архивирован или дубликат
- `ValueError` — если валидация не прошла

---

### Обновление distribute_upd()

**Новый параметр:** `user_id` — для логирования в историю

**Изменения:**
- После распределения создаётся запись истории
- Action = "CREATE"
- old_distribution = None
- new_distribution = JSON с распределением

**Применение:**
- Отслеживание первичного распределения
- Контроль изменений с самого начала

---

## 📡 Новые API endpoints

### GET /api/v1/material-costs/{upd_id}/distribution-history

Получение истории распределения УПД

**Доступ:** ACCOUNTANT, MATERIALS_MANAGER, MANAGER

**Параметры:**
- `limit` (query, optional) — макс. записей (по умолчанию 50)

**Запрос:**
```bash
GET /api/v1/material-costs/15/distribution-history?limit=20
Authorization: Bearer <token>
```

**Ответ:**
```json
{
  "upd_id": 15,
  "document_number": "УПД-2026-001",
  "history": [
    {
      "id": 3,
      "action": "UPDATE",
      "user_id": 5,
      "old_distribution": {
        "distributions": [
          {
            "material_cost_item_id": 10,
            "material_request_id": 20,
            "distributed_quantity": 100.0,
            "distributed_amount": 15000.0
          }
        ],
        "total_amount": 15000.0
      },
      "new_distribution": {
        "distributions": [
          {
            "material_cost_item_id": 10,
            "material_request_id": 25,
            "distributed_quantity": 100.0,
            "distributed_amount": 15000.0
          }
        ],
        "total_amount": 15000.0
      },
      "description": "Корректировка распределения УПД №УПД-2026-001",
      "created_at": "2026-01-26T15:30:00"
    },
    {
      "id": 1,
      "action": "CREATE",
      "user_id": 3,
      "old_distribution": null,
      "new_distribution": {
        "distributions": [
          {
            "material_cost_item_id": 10,
            "material_request_id": 20,
            "distributed_quantity": 100.0,
            "distributed_amount": 15000.0
          }
        ],
        "total_amount": 15000.0
      },
      "description": "Первичное распределение УПД №УПД-2026-001",
      "created_at": "2026-01-26T10:00:00"
    }
  ],
  "total_changes": 2
}
```

**Применение:**
- Просмотр всех изменений УПД
- Аудит для бухгалтерии
- Выявление ошибок

---

### PUT /api/v1/material-costs/{upd_id}/redistribute

Корректировка распределения УПД

**Доступ:** MATERIALS_MANAGER, MANAGER

**Тело запроса:** то же что и для `/distribute` (DistributeUPDRequest)

**Запрос:**
```bash
PUT /api/v1/material-costs/15/redistribute
Authorization: Bearer <token>
Content-Type: application/json

{
  "distributions": [
    {
      "material_cost_item_id": 10,
      "material_request_id": 25,
      "distributed_quantity": 100.0,
      "distributed_amount": 15000.0
    }
  ]
}
```

**Ответ:**
```json
{
  "upd_id": 15,
  "new_status": "DISTRIBUTED",
  "total_distributed_amount": 15000.0,
  "distributions_count": 1,
  "cost_entries_created": 1
}
```

**Ошибки:**
- `400` — УПД архивирован или дубликат
- `400` — валидация не прошла
- `404` — УПД не найден

**Интеграция:**
- ✅ Логируется в `audit_log`
- ✅ Логируется в `upd_distribution_history`
- ✅ WebSocket уведомление (upd_redistributed)
- ✅ Пересчёт затрат по объектам

---

## 🔔 Новое WebSocket уведомление

### upd_redistributed

**Событие:** корректировка распределения УПД

**Получатели:** ACCOUNTANT, MANAGER

**Сообщение:**
```json
{
  "type": "notification",
  "notification_type": "upd_redistributed",
  "title": "УПД корректирован",
  "message": "Распределение УПД №УПД-2026-001 изменено. Новая сумма: 15000.00 ₽",
  "data": {
    "upd_id": 15,
    "document_number": "УПД-2026-001",
    "total_distributed": 15000.0,
    "distributions_count": 1,
    "user_id": 5
  },
  "timestamp": "2026-01-26T15:30:00"
}
```

**Применение:**
- Немедленное оповещение о корректировке
- Бухгалтер видит изменения в реальном времени
- Руководитель контролирует корректировки

---

## 📊 Обновлённая база данных

**Количество таблиц:** 20 (было 19)

**Новая таблица:**
- `upd_distribution_history` — история изменений распределения

**Поля:**
- `id` — первичный ключ
- `material_cost_id` — ссылка на УПД
- `user_id` — кто изменил
- `action` — тип действия (CREATE, UPDATE, DELETE)
- `old_distribution` — старое распределение (JSON)
- `new_distribution` — новое распределение (JSON)
- `description` — описание изменения
- `created_at` — когда изменено
- `updated_at` — последнее обновление

**Индексы:**
```sql
CREATE INDEX idx_upd_distribution_history_upd 
ON upd_distribution_history(material_cost_id);

CREATE INDEX idx_upd_distribution_history_created 
ON upd_distribution_history(created_at DESC);
```

---

## 🎯 Сценарии использования

### Сценарий 1: Первичное распределение с историей

```bash
# 1. Менеджер загружает и распределяет УПД
POST /api/v1/material-costs/upload
→ УПД #15 создан

POST /api/v1/material-costs/15/distribute
→ Распределён на объект #5
→ Создана запись истории (action=CREATE)

# 2. Бухгалтер просматривает историю
GET /api/v1/material-costs/15/distribution-history
→ 1 запись: первичное распределение
```

---

### Сценарий 2: Корректировка распределения

```bash
# 1. Менеджер обнаружил ошибку в распределении
# (должен быть объект #10, а не #5)

GET /api/v1/material-costs/15
→ Текущее распределение: объект #5

# 2. Просмотр истории
GET /api/v1/material-costs/15/distribution-history
→ 1 запись: CREATE (объект #5)

# 3. Корректировка распределения
PUT /api/v1/material-costs/15/redistribute
Body: { distributions: [{ объект #10 }] }

→ Старое распределение удалено
→ Новое распределение создано
→ Затраты пересчитаны
→ История обновлена (action=UPDATE)
→ WebSocket: upd_redistributed

# 4. Проверка истории
GET /api/v1/material-costs/15/distribution-history
→ 2 записи:
  - UPDATE: объект #5 → #10
  - CREATE: объект #5 (первичное)
```

---

### Сценарий 3: Множественные корректировки

```bash
# День 1: Первичное распределение
POST /api/v1/material-costs/20/distribute
→ Объект #1, сумма 100000 ₽
→ История: CREATE

# День 2: Первая корректировка (нашли ошибку)
PUT /api/v1/material-costs/20/redistribute
→ Объект #2, сумма 100000 ₽
→ История: UPDATE (#1 → #2)

# День 3: Вторая корректировка (разделили на 2 объекта)
PUT /api/v1/material-costs/20/redistribute
→ Объект #2 (60000 ₽) + Объект #3 (40000 ₽)
→ История: UPDATE (1 объект → 2 объекта)

# Итоговая история
GET /api/v1/material-costs/20/distribution-history
→ 3 записи:
  1. UPDATE: 1 объект → 2 объекта (День 3)
  2. UPDATE: объект #1 → #2 (День 2)
  3. CREATE: объект #1 (День 1)
```

---

## 🔒 Обновлённые права доступа

| Действие | ACCOUNTANT | MATERIALS_MANAGER | MANAGER | ADMIN |
|----------|------------|-------------------|---------|-------|
| Загрузка УПД | ✅ | ✅ | ❌ | ✅ |
| Распределение УПД | ❌ | ✅ | ❌ | ✅ |
| Корректировка распределения | ❌ | ✅ | ✅ | ✅ |
| Просмотр истории | ✅ | ✅ | ✅ | ✅ |
| WebSocket уведомления | ✅ | ✅ | ✅ | ✅ |

---

## ✨ Финальные итоги

**Добавлено в этом обновлении:**
- ✅ Таблица `upd_distribution_history` (20-я таблица)
- ✅ 3 новых метода в UPDService:
  - `log_distribution_history()` — логирование изменений
  - `get_distribution_history()` — получение истории
  - `redistribute_upd()` — корректировка распределения
- ✅ Обновлён `distribute_upd()` для логирования
- ✅ 2 новых API endpoint:
  - `GET /{upd_id}/distribution-history` — история
  - `PUT /{upd_id}/redistribute` — корректировка
- ✅ WebSocket уведомление `upd_redistributed`
- ✅ Полная интеграция с audit_log
- ✅ Пересчёт затрат при корректировке

**Всего в модуле УПД:**
- ✅ Обнаружение дубликатов (статус DUPLICATE)
- ✅ История распределения (20 таблиц БД)
- ✅ Корректировка распределения
- ✅ 4 типа WebSocket уведомлений
- ✅ 7 API endpoints

**Файлы изменены:**
- `app/models.py` — UPDDistributionHistory модель
- `app/upd/service.py` — 3 новых метода + обновлён distribute_upd (~220 строк)
- `app/upd/router.py` — 2 новых endpoint (~130 строк)
- База данных — 20 таблиц

**Строк кода:** ~350 новых строк

---

## 🚀 Готово к использованию

**Сервер запущен:** ✅ http://127.0.0.1:8000  
**Swagger UI:** http://127.0.0.1:8000/docs  
**WebSocket:** ws://127.0.0.1:8000/api/v1/ws  
**БД:** 20 таблиц, готова к работе

**Все задачи выполнены:**
- ✅ Обнаружение дубликатов УПД
- ✅ История распределения УПД
- ✅ Корректировка распределения УПД
- ✅ WebSocket уведомления для УПД

**Система полностью готова к продакшену!**
