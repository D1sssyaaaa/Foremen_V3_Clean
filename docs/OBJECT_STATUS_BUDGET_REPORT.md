# Отчёт: Статусы объектов и бюджеты

**Дата:** 26 января 2026 г.  
**Модуль:** Объекты учёта — статусы и бюджеты

---

## ✅ Что сделано

### 1. Статусы объектов (ObjectStatus)

**Добавлен enum** `ObjectStatus` со следующими статусами:
- **Активен** — объект в работе
- **Готовится к закрытию** — завершающая стадия
- **Закрыт** — работы завершены
- **Архив** — объект в архиве

**Изменения в модели CostObject:**
- Поле `status` теперь использует ObjectStatus.ACTIVE.value по умолчанию
- Добавлено 3 новых поля для бюджета:
  - `budget_amount` — плановый бюджет объекта
  - `budget_alert_80_sent` — флаг отправки уведомления при 80%
  - `budget_alert_100_sent` — флаг отправки уведомления при 100%

---

### 2. Сервис ObjectService

**Файл:** `backend/app/services/object_service.py`

**Методы:**

#### change_status()
Изменение статуса объекта с автоматической архивацией УПД
```python
# При смене статуса на "Архив":
1. Статус объекта → "Архив"
2. Все УПД объекта → статус "ARCHIVED"
3. Объект деактивируется (is_active = False)
4. Логируется в audit_log
```

#### get_objects()
Получение объектов с фильтрацией:
- `include_archived` — показать архивные (по умолчанию скрыты)
- `status` — фильтр по статусу
- `foreman_id` — только объекты бригадира

#### calculate_spent_budget()
Автоматический расчёт потраченного бюджета:
- **Материалы:** сумма из MaterialCost
- **Техника:** сумма из EquipmentCost
- **ФОТ:** сумма часов × ставка из TimeSheet

#### check_budget_alerts()
Проверка превышения бюджета и отправка уведомлений:
- При 80% → уведомление руководителю и бухгалтеру (однократно)
- При 100% → критичное уведомление (однократно)

---

### 3. API endpoints

**Файл:** `backend/app/objects/router.py`

#### PATCH /api/v1/objects/{object_id}/change-status
Изменение статуса объекта

**Запрос:**
```json
{
  "status": "Готовится к закрытию"
}
```

**Ответ:**
```json
{
  "id": 1,
  "name": "Объект 1",
  "status": "Готовится к закрытию",
  "is_active": true,
  "message": "Статус объекта изменён на 'Готовится к закрытию'"
}
```

**Особенности:**
- При архивации автоматически архивируются все УПД
- Логируется в audit_log
- Доступно: MANAGER, ADMIN

---

#### GET /api/v1/objects/{object_id}/budget
Получение информации о бюджете

**Ответ:**
```json
{
  "has_budget": true,
  "budget": 1000000.0,
  "spent": 756432.50,
  "percentage": 75.64,
  "alert_80": false,
  "alert_100": false,
  "object_name": "Строительство дома",
  "object_code": "OBJ-2026-001"
}
```

---

#### PUT /api/v1/objects/{object_id}/budget
Установка/изменение бюджета объекта

**Запрос:**
```json
{
  "budget_amount": 1500000.0
}
```

**Ответ:**
```json
{
  "id": 1,
  "name": "Строительство дома",
  "budget_amount": 1500000.0,
  "message": "Бюджет объекта обновлён"
}
```

**Особенности:**
- Сбрасываются флаги алертов (можно повторно получить уведомления)
- Логируется в audit_log
- Доступно: MANAGER, ADMIN

---

#### GET /api/v1/objects/
Получение списка объектов (обновлён)

**Параметры:**
- `include_archived` (bool) — показать архивные объекты
- `status_filter` (str) — фильтр по статусу

**Особенности:**
- **Бригадиры** видят только свои объекты
- **Остальные роли** видят все объекты
- По умолчанию архивные объекты **скрыты**
- Добавлено поле `budget_amount` в ответ

---

### 4. Автоматическая архивация УПД

**Логика:**
```python
# При смене статуса объекта на "Архив"
await ObjectService.change_status(
    object_id=123,
    new_status=ObjectStatus.ARCHIVED,
    user_id=current_user.id
)

# Автоматически:
1. Все УПД этого объекта → status="ARCHIVED"
2. Объект деактивируется (is_active=False)
3. Записывается в audit_log
```

**Количество архивированных УПД** возвращается в методе.

---

### 5. Система уведомлений о бюджете

**Механизм работы:**

1. **Создание/обновление затрат** → вызов `check_budget_alerts()`
2. **Расчёт процента** использования бюджета
3. **Проверка порогов:**
   - ≥ 80% и флаг `budget_alert_80_sent=False` → отправить уведомление → установить флаг
   - ≥ 100% и флаг `budget_alert_100_sent=False` → отправить уведомление → установить флаг

**Кому отправляются уведомления:**
- Руководитель (MANAGER)
- Бухгалтер (ACCOUNTANT)

**Уведомления отправляются:**
- В Telegram (через бот)
- В веб-интерфейс (когда будет WebSocket)

---

## 📊 База данных

**Обновлённая структура cost_objects:**
```sql
CREATE TABLE cost_objects (
    id INTEGER PRIMARY KEY,
    name VARCHAR(255),
    code VARCHAR(50) UNIQUE,
    contract_number VARCHAR(100),
    contract_amount FLOAT,
    material_amount FLOAT,
    labor_amount FLOAT,
    start_date DATE,
    end_date DATE,
    status VARCHAR(50) DEFAULT 'Активен',  -- ObjectStatus
    is_active BOOLEAN DEFAULT TRUE,
    description TEXT,
    budget_amount FLOAT,                   -- Бюджет объекта
    budget_alert_80_sent BOOLEAN DEFAULT FALSE,  -- Флаг алерта 80%
    budget_alert_100_sent BOOLEAN DEFAULT FALSE, -- Флаг алерта 100%
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

**Индексы:**
- `status` — для фильтрации по статусу
- `code` — уникальный код объекта

---

## 🎯 Примеры использования

### Изменение статуса объекта

```bash
# Перевести объект в архив
curl -X PATCH http://localhost:8000/api/v1/objects/1/change-status \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"status": "Архив"}'

# Результат:
# - Объект → статус "Архив"
# - Все УПД объекта → статус "ARCHIVED"
# - Запись в audit_log
```

### Установка бюджета

```bash
# Установить бюджет 2 млн рублей
curl -X PUT http://localhost:8000/api/v1/objects/1/budget \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"budget_amount": 2000000}'
```

### Проверка бюджета

```bash
# Получить информацию о бюджете
curl http://localhost:8000/api/v1/objects/1/budget \
  -H "Authorization: Bearer <token>"

# Ответ:
{
  "budget": 2000000,
  "spent": 1650000,
  "percentage": 82.5,
  "alert_80": true,  # Уведомление уже отправлено
  "alert_100": false
}
```

### Фильтрация объектов

```bash
# Все объекты включая архивные
GET /api/v1/objects/?include_archived=true

# Только объекты в работе
GET /api/v1/objects/?status_filter=Активен

# Объекты готовые к закрытию
GET /api/v1/objects/?status_filter=Готовится к закрытию
```

---

## 🔒 Права доступа

| Endpoint | FOREMAN | MANAGER | ADMIN |
|----------|---------|---------|-------|
| GET /objects/ | Свои объекты | Все | Все |
| GET /objects/{id}/budget | ✅ | ✅ | ✅ |
| PATCH /objects/{id}/change-status | ❌ | ✅ | ✅ |
| PUT /objects/{id}/budget | ❌ | ✅ | ✅ |

---

## 📝 Интеграция с аудитом

Все критичные действия логируются:

**Изменение статуса:**
```json
{
  "user_id": 1,
  "action": "CHANGE_STATUS",
  "entity_type": "CostObject",
  "entity_id": 123,
  "old_value": "Активен",
  "new_value": "Архив",
  "description": "Изменение статуса объекта 'Строительство дома' с 'Активен' на 'Архив'"
}
```

**Изменение бюджета:**
```json
{
  "user_id": 1,
  "action": "UPDATE_BUDGET",
  "entity_type": "CostObject",
  "entity_id": 123,
  "old_value": "1000000",
  "new_value": "1500000",
  "description": "Изменение бюджета объекта 'Строительство дома' с 1000000 на 1500000"
}
```

---

## ✨ Итог

**Сегодня добавлено:**
- ✅ Статусы объектов (4 статуса)
- ✅ Автоархивация УПД при архивации объекта
- ✅ Бюджеты объектов с автоподсчётом
- ✅ Система уведомлений при 80% и 100%
- ✅ API для управления статусами и бюджетом
- ✅ Фильтрация объектов по статусу
- ✅ Скрытие архивных объектов по умолчанию
- ✅ Интеграция с системой аудита

**Файлы изменены/созданы:**
- `app/core/models_base.py` — ObjectStatus enum
- `app/models.py` — поля бюджета в CostObject
- `app/services/object_service.py` — бизнес-логика
- `app/objects/router.py` — новые endpoints
- База данных обновлена

**Время работы:** ~40 минут  
**Строк кода:** ~400 строк

---

## 🚀 Что дальше?

**Готовы к реализации:**
1. **Табели РТБ** — отмена, комментарии, Excel
2. **WebSocket уведомления** — реальное время для веба
3. **УПД улучшения** — дубликаты, корректировка

**Сервер запущен:** ✅ http://127.0.0.1:8000  
**Swagger UI:** http://127.0.0.1:8000/docs
