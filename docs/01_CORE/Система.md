ПОЛНОЕ ТЕХНИЧЕСКОЕ ЗАДАНИЕ: СИСТЕМА УЧЕТА ЗАТРАТ СТРОИТЕЛЬНОЙ КОМПАНИИ
1. ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА
1.1. Цели проекта
Создание единой платформы для управленческого учёта всех затрат строительной компании с распределением по объектам, автоматизацией рутинных операций и обеспечением мобильности для полевых сотрудников через Telegram-бота.

1.2. Ключевые проблемы, решаемые системой
Разрозненность учёта (Excel, бумажные документы, устные заявки)

Отсутствие контроля за использованием арендованной техники и инструмента

Сложность учёта рабочего времени бригад и связанных с этим затрат на ФОТ

Непрозрачность затрат по объектам для руководства

Низкая скорость обработки документов (УПД, заявки)

1.3. Ожидаемые результаты
Сокращение времени обработки документов на 50%

Повышение прозрачности расходов по объектам на 80%

Уменьшение ошибок учёта на 70%

Возможность оперативного принятия решений на основе актуальных данных

Удобство работы для всех категорий пользователей

2. АРХИТЕКТУРА СИСТЕМЫ
2.1. Высокоуровневая схема
text
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Клиенты:      │     │   API Gateway   │     │   Микросервисы  │
│ • Веб (React)   │────▶│   (FastAPI +    │────▶│   • Auth        │
│ • Telegram Bot  │     │     Nginx)      │     │   • Costs       │
└─────────────────┘     └─────────────────┘     │   • Equipment   │
                                                 │   • Planning    │
                                                 │   • Analytics   │
                                                 └─────────────────┘
                                                         │
                                                         ▼
                                                 ┌─────────────────┐
                                                 │    Хранение:    │
                                                 │ • PostgreSQL    │
                                                 │ • Redis         │
                                                 │ • MinIO/S3      │
                                                 └─────────────────┘
2.2. Технологический стек
Компонент	Технология	Обоснование
Backend	Python 3.11 + FastAPI	Асинхронность, автоматическая документация OpenAPI
Frontend	React 18 + TypeScript + Vite	Типобезопасность, быстрая сборка
Telegram Bot	Python + aiogram	Асинхронный, современный API
База данных	PostgreSQL 15 + Redis 7	Надёжность, JSONB, кэширование
Хранилище файлов	MinIO/S3	S3-совместимое, для XML УПД
Контейнеризация	Docker + Docker Compose	Единое окружение, изоляция
3. БИЗНЕС-СУЩНОСТИ И ПРОЦЕССЫ
3.1. Основные потоки данных
Поток 1: Учёт рабочего времени бригад (РТБ)
text
Бригадир (Telegram) → Создаёт список бригады → Еженедельно подаёт табель
→ Ответственный (HR_MANAGER) проверяет → Утверждает/редактирует
→ Система рассчитывает ФОТ → Данные попадают в отчётность объекта
Поток 2: Аренда техники и инструмента
text
Бригадир (Telegram) → Заявка на аренду → Менеджер (EQUIPMENT_MANAGER) утверждает
→ Указывает поставщика → Техника работает → Бригадир подаёт часы работы
→ Менеджер указывает стоимость → Система создаёт затраты → Отчётность объекта
Поток 3: Заявки на материалы и инертные материалы
text
Бригадир (Telegram) → Заявка на материалы → Менеджер (PROCUREMENT_MANAGER) согласовывает
→ Менеджер (MATERIALS_MANAGER) работает с поставщиком → Поступает УПД
→ Менеджер распределяет УПД по заявкам → Система обновляет статусы → Отчётность
Поток 4: Обработка УПД от поставщиков
text
Бухгалтер (Веб) → Загружает XML УПД → Система парсит данные
→ Менеджер (MATERIALS_MANAGER) распределяет строки по заявкам/объектам
→ Система создаёт материальные затраты → Отчётность объектов
3.2. Детализация процессов
3.2.1. Процесс учёта времени бригад (РТБ)
Участники: Бригадир, HR_MANAGER
Входные данные: Список бригады (ФИО, телефон), часы работы по дням/объектам
Выходные данные: Утверждённый табель, расчёт ФОТ

Этапы:

Формирование бригады: Бригадир через бота создаёт список сотрудников

Подача табеля: Еженедельно (пятница) бригадир подаёт часы через:

Excel-шаблон (длинный формат: ФИО, Дата, Объект, Часы)

Пошаговый ввод в боте (резервный способ)

Нумерация: Автоматическая, формат "РТБ-001" (счётчик сбрасывается каждый год)

Проверка: HR_MANAGER проверяет, при необходимости редактирует

Утверждение: HR_MANAGER утверждает → данные попадают в систему

Расчёт ФОТ: HR_MANAGER указывает ставку за час → система рассчитывает сумму

Статусы табеля:

ЧЕРНОВИК (заполняется)

НА РАССМОТРЕНИИ (отправлен на проверку)

ОТКОРРЕКТИРОВАННЫЙ (HR_MANAGER внёс правки)

УТВЕРЖДЁННЫЙ (финальный статус)

3.2.2. Процесс аренды техники/инструмента
Участники: Бригадир, EQUIPMENT_MANAGER
Входные данные: Тип оборудования, период, объект, комментарий
Выходные данные: Затраты на аренду, статистика использования

Этапы:

Заявка: Бригадир создаёт заявку через бота

Утверждение: EQUIPMENT_MANAGER утверждает, указывает поставщика

Работа: Оборудование используется на объекте

Учёт времени: Бригадир подаёт фактические часы работы

Расчёт стоимости: EQUIPMENT_MANAGER указывает:

Стоимость за час

Галочки "Включить доставку", "Включить штраф" (с указанием сумм)

Формирование затрат: Система рассчитывает итоговую сумму

Особенности:

Один тип техники можно заказать на несколько объектов одновременно

Отмена заявки в любой статус с обязательным указанием причины

Двухэтапная отмена: бригадир → EQUIPMENT_MANAGER подтверждает

3.2.3. Процесс заявок на материалы
Участники: Бригадир, PROCUREMENT_MANAGER, MATERIALS_MANAGER
Входные данные: Список материалов, количество, срочность, адрес доставки
Выходные данные: Статус заявки, привязанные УПД

Этапы:

Создание: Бригадир создаёт заявку через бота

Согласование: PROCUREMENT_MANAGER согласовывает

Обработка: MATERIALS_MANAGER работает с поставщиками

Поступление УПД: Бухгалтер загружает УПД в систему

Распределение: MATERIALS_MANAGER распределяет строки УПД по заявкам

Закрытие: MATERIALS_MANAGER закрывает заявку при полной поставке

Статусы заявки:

НОВАЯ

НА СОГЛАСОВАНИИ

В ОБРАБОТКЕ

ЗАКАЗАНО

ЧАСТИЧНО ПОСТАВЛЕНО (автоматически при привязке первого УПД)

ОТГРУЖЕНО (автоматически при закрытии всех позиций)

ВЫПОЛНЕНА (ручное закрытие менеджером)

3.2.4. Процесс обработки УПД
Участники: Бухгалтер, MATERIALS_MANAGER
Входные данные: XML-файл УПД
Выходные данные: Распределённые материальные затраты по объектам

Этапы:

Загрузка: Бухгалтер загружает XML через веб-интерфейс

Парсинг: Система автоматически извлекает данные (поставщик, номенклатура, суммы)

Распределение: MATERIALS_MANAGER в матричном интерфейсе:

Видит все строки УПД

Для каждой строки выбирает заявку (и автоматически объект)

Распределяет одну строку на несколько заявок при необходимости

Формирование затрат: Система создаёт записи материальных затрат

Статусы УПД:

NEW (загружен, не обработан)

DISTRIBUTED (распределён по заявкам)

ARCHIVED (все связанные заявки закрыты)

4. ПОЛЬЗОВАТЕЛЬСКИЕ РОЛИ И ПРАВА
4.1. Роли и их назначение
Роль	Описание	Основные функции
FOREMAN (Бригадир)	Полевой сотрудник, руководитель бригады	Только Telegram-бот: заявки на материалы/технику, подача табелей РТБ
ACCOUNTANT (Бухгалтер)	Работа с финансовыми документами	Веб: загрузка УПД, просмотр отчётов, экспорт данных
HR_MANAGER (Менеджер по персоналу)	Учёт рабочего времени, ФОТ	Веб+Telegram: проверка табелей РТБ, указание ставок
EQUIPMENT_MANAGER (Менеджер по технике)	Управление арендой техники/инструмента	Веб+Telegram: утверждение заявок, указание стоимости, штрафов
MATERIALS_MANAGER (Менеджер по снабжению)	Работа с поставщиками материалов	Веб+Telegram: обработка заявок, распределение УПД
PROCUREMENT_MANAGER (Менеджер по закупкам)	Согласование закупок материалов	Веб+Telegram: согласование заявок на материалы
MANAGER (Руководитель)	Общее управление, контроль	Веб+Telegram: полный доступ, аналитика, назначение ролей
ADMIN (Администратор)	Техническое управление системой	Веб: настройки системы, управление пользователями, мониторинг
4.2. Матрица прав доступа
Право / Роль	FOREMAN	ACCOUNTANT	HR_M	EQUIP_M	MAT_M	PROC_M	MANAGER	ADMIN
Просмотр своих объектов	✅	❌	❌	❌	❌	❌	✅	✅
Создание заявок на материалы	✅	❌	❌	❌	❌	❌	✅	✅
Создание заявок на технику	✅	❌	❌	❌	❌	❌	✅	✅
Подача табелей РТБ	✅	❌	❌	❌	❌	❌	❌	❌
Загрузка УПД	❌	✅	❌	❌	❌	❌	✅	✅
Проверка табелей РТБ	❌	❌	✅	❌	❌	❌	✅	✅
Утверждение аренды техники	❌	❌	❌	✅	❌	❌	✅	✅
Обработка заявок на материалы	❌	❌	❌	❌	✅	❌	✅	✅
Согласование закупок	❌	❌	❌	❌	❌	✅	✅	✅
Распределение УПД	❌	❌	❌	❌	✅	❌	✅	✅
Просмотр финансовой аналитики	❌	✅	❌	❌	❌	❌	✅	✅
Назначение ролей пользователям	❌	❌	❌	❌	❌	❌	✅	✅
Системные настройки	❌	❌	❌	❌	❌	❌	❌	✅
4.3. Комбинирование ролей
Одному пользователю можно назначить несколько ролей

На одну роль можно назначить несколько пользователей

При комбинировании прав суммируются

Обязательная настройка уведомлений в профиле для отключения ненужных оповещений

5. СТРУКТУРА БАЗЫ ДАННЫХ
5.1. Основные таблицы и связи
sql
-- Ядро системы: пользователи и объекты
users (id, username, phone, role[], telegram_chat_id, ...)
cost_objects (id, name, code, contract_number, contract_amount, ...)
object_foremen (object_id, foreman_id) -- связь бригадиров с объектами

-- Бригады и табели (РТБ)
brigades (id, foreman_id, name)
brigade_members (id, brigade_id, full_name, phone)
time_sheets (id, brigade_id, period_start, period_end, status, ...)
time_sheet_items (id, time_sheet_id, member_id, date, cost_object_id, hours)

-- Аренда техники и инструмента
equipment_orders (id, cost_object_id, foreman_id, type, equipment_type, 
                  start_date, end_date, status, supplier, ...)
equipment_costs (id, equipment_order_id, hours_worked, hour_rate, 
                 delivery_included, penalty_included, total_amount, ...)

-- Заявки на материалы
material_requests (id, cost_object_id, foreman_id, status, urgency, 
                   material_type, delivery_time, delivery_address, ...)
                   -- material_type: VARCHAR(20), 'regular' | 'inert'
                   -- delivery_time: VARCHAR(50), обязательно для inert
material_request_items (id, request_id, material_name, quantity, unit)

-- УПД и материальные затраты
material_costs (id, cost_object_id, supplier_name, document_number, 
                document_date, total_amount, ...)
material_cost_items (id, material_cost_id, product_name, quantity, 
                     price, amount, ...)
upd_distribution (id, material_cost_id, material_request_id, 
                  distributed_quantity, distributed_amount)

-- Общие затраты (наследование)
cost_entries (id, type, cost_object_id, date, amount, description, ...)
-- Наследующие таблицы: equipment_costs, material_costs, other_costs

-- Задачи и планирование
tasks (id, cost_object_id, title, start_date, due_date, status, ...)
task_assignees (task_id, user_id)

-- Аудит и логи
audit_log (id, user_id, entity_type, entity_id, action, old_values, new_values, ...)
telegram_notifications (id, user_id, notification_type, message_text, status, ...)
5.2. Ключевые индексы
sql
-- Для быстрого поиска по объектам
CREATE INDEX idx_cost_entries_object ON cost_entries(cost_object_id, date);
CREATE INDEX idx_material_costs_object ON material_costs(cost_object_id);
CREATE INDEX idx_equipment_orders_object ON equipment_orders(cost_object_id);

-- Для работы с табелями
CREATE INDEX idx_time_sheets_brigade ON time_sheets(brigade_id, period_start);
CREATE INDEX idx_time_sheet_items_date ON time_sheet_items(date, cost_object_id);

-- Для статусных запросов
CREATE INDEX idx_equipment_orders_status ON equipment_orders(status);
CREATE INDEX idx_material_requests_status ON material_requests(status);
CREATE INDEX idx_time_sheets_status ON time_sheets(status);
6. API СПЕЦИФИКАЦИЯ
6.1. Основные группы эндпоинтов
Аутентификация и пользователи
text
POST   /api/v1/auth/login              # Вход в систему
POST   /api/v1/auth/telegram/activate  # Привязка Telegram
GET    /api/v1/users/me                # Данные текущего пользователя
PUT    /api/v1/users/notifications     # Настройка уведомлений
Объекты учета
text
GET    /api/v1/objects                 # Список объектов
GET    /api/v1/objects/{id}/summary    # Сводка по объекту
GET    /api/v1/objects/{id}/costs      # Затраты по объекту (фильтры: период, тип)
Табели РТБ (рабочее время бригад)
text
GET    /api/v1/time-sheets             # Список табелей (фильтры: статус, период)
POST   /api/v1/time-sheets             # Создать табель (из Excel/бота)
PUT    /api/v1/time-sheets/{id}        # Редактировать табель
POST   /api/v1/time-sheets/{id}/submit # Отправить на проверку
POST   /api/v1/time-sheets/{id}/approve # Утвердить табель
POST   /api/v1/time-sheets/{id}/reject # Отклонить с комментарием
PUT    /api/v1/time-sheets/{id}/rate   # Указать ставку за час
Аренда техники/инструмента
text
GET    /api/v1/equipment-orders        # Список заявок
POST   /api/v1/equipment-orders        # Создать заявку (из бота)
POST   /api/v1/equipment-orders/{id}/approve # Утвердить заявку
POST   /api/v1/equipment-orders/{id}/reject  # Отклонить заявку
POST   /api/v1/equipment-orders/{id}/cancel  # Запрос на отмену
POST   /api/v1/equipment-orders/{id}/supplier # Указать поставщика
PUT    /api/v1/equipment-orders/{id}/cost    # Указать стоимость
POST   /api/v1/equipment-orders/{id}/hours   # Учесть отработанные часы
Заявки на материалы
text
GET    /api/v1/material-requests       # Список заявок
POST   /api/v1/material-requests       # Создать заявку (из бота)
POST   /api/v1/material-requests/{id}/approve # Согласовать заявку
POST   /api/v1/material-requests/{id}/process # Взять в обработку
PUT    /api/v1/material-requests/{id}/status  # Изменить статус
УПД и материальные затраты
text
POST   /api/v1/material-costs/upload   # Загрузить XML УПД
GET    /api/v1/material-costs/unprocessed # Необработанные УПД
POST   /api/v1/material-costs/{id}/distribute # Распределить УПД
PUT    /api/v1/material-costs/{id}     # Редактировать распределение
GET    /api/v1/material-costs/export   # Экспорт данных
Аналитика и отчеты
text
GET    /api/v1/analytics/object/{id}   # Аналитика по объекту
GET    /api/v1/analytics/cost-structure # Структура затрат
GET    /api/v1/analytics/equipment-usage # Использование техники
GET    /api/v1/analytics/period-report # Отчет за период
POST   /api/v1/analytics/export        # Экспорт отчета (Excel/PDF)
Telegram Bot Webhooks
text
POST   /api/v1/telegram/webhook        # Входящие сообщения от бота
POST   /api/v1/telegram/send           # Отправка уведомления через бота
GET    /api/v1/telegram/user/{id}/state # Получение состояния пользователя в боте
6.2. WebSocket endpoints
text
/ws/notifications    # Уведомления в реальном времени
/ws/task-updates     # Обновления задач
/ws/cost-updates     # Обновления затрат
7. TELEGRAM BOT АРХИТЕКТУРА
7.1. Состояния (Finite State Machine)
python
class BotStates:
    # Основные состояния
    MAIN_MENU = "main_menu"
    
    # Табели РТБ
    RTS_SELECT_PERIOD = "rts:select_period"
    RTS_SELECT_MEMBER = "rts:select_member"
    RTS_ENTER_HOURS = "rts:enter_hours"
    RTS_UPLOAD_EXCEL = "rts:upload_excel"
    
    # Аренда техники
    EQ_SELECT_TYPE = "eq:select_type"
    EQ_SELECT_OBJECT = "eq:select_object"
    EQ_ENTER_DATES = "eq:enter_dates"
    EQ_ENTER_COMMENT = "eq:enter_comment"
    
    # Заявки на материалы
    MR_SELECT_TYPE = "mr:select_type"  # обычные/инертные
    MR_SELECT_OBJECT = "mr:select_object"
    MR_ENTER_MATERIALS = "mr:enter_materials"
    MR_ENTER_URGENCY = "mr:enter_urgency"
    MR_ENTER_ADDRESS = "mr:enter_address"
    
    # Просмотр активных заявок
    VIEW_ACTIVE_REQUESTS = "view:active_requests"
    VIEW_REQUEST_DETAILS = "view:request_details"
7.2. Основные команды и клавиатуры
Главное меню (для бригадира)
text
🎯 МОИ ЗАДАЧИ
📋 ЗАЯВКА НА МАТЕРИАЛЫ
🚜 ЗАЯВКА НА ТЕХНИКУ
🛠️ ЗАКАЗ ИНСТРУМЕНТА
📊 ПОДАТЬ ТАБЕЛЬ РТБ
📋 АКТИВНЫЕ ЗАЯВКИ
👥 УПРАВЛЕНИЕ БРИГАДОЙ
ℹ️ ПОМОЩЬ
Меню для менеджеров
text
📋 НОВЫЕ ЗАЯВКИ (3)
🚜 ТЕХНИКА НА УТВЕРЖДЕНИИ (2)
📊 ТАБЕЛИ НА ПРОВЕРКЕ (5)
📄 УПД ДЛЯ РАСПРЕДЕЛЕНИЯ (7)
⚙️ НАСТРОЙКИ
7.3. Форматы сообщений
Уведомление о новой заявке на материалы
text
🆕 НОВАЯ ЗАЯВКА НА МАТЕРИАЛЫ

№: MR-2024-001
Объект: ЖК "Северный"
Бригадир: Иванов И.И. (+7-XXX-XXX-XX-XX)
Срочность: 🔥 СРОЧНО
Адрес доставки: ул. Строителей, 15, к 10:00

Материалы:
• Кабель ВВГ 3x2.5 - 100 м
• Муфты кабельные - 5 шт

[ПРОСМОТРЕТЬ] [СОГЛАСОВАТЬ] [ЗАПРОСИТЬ ДЕТАЛИ]
Уведомление о корректировке табеля
text
🔄 ТАБЕЛЬ СКОРРЕКТИРОВАН

РТБ-2024-001 (20-26 января 2024)

ИЗМЕНЕНИЯ:
• Иванов И.И. (20.01): 10ч → 8ч (Северный)
• Петров П.П. (21.01): +2ч (Южный, стало 10ч)

Ответственный: Петров П.П. +7-XXX-XXX-XX-XX

[ПОДТВЕРДИТЬ ОЗНАКОМЛЕНИЕ]
Напоминание о заполнении поставщика
text
⏰ НЕ ЗАПОЛНЕН ПОСТАВЩИК

Заявка №EQ-2024-001 (Экскаватор)
Укажите поставщика: _______________

[ООО ТЕХНОРЕНТ] [ИП ИВАНОВ] [ВВЕСТИ ВРУЧНУЮ]
7.4. Офлайн-режим
javascript
// Локальное хранилище действий
class OfflineStorage {
    constructor(userId) {
        this.userId = userId;
        this.pendingActions = [];
    }
    
    addAction(type, data) {
        this.pendingActions.push({
            id: this.generateId(),
            type: type,
            data: data,
            timestamp: new Date().toISOString(),
            status: 'pending'
        });
        this.saveToLocalStorage();
    }
    
    async syncWhenOnline() {
        const pending = this.pendingActions.filter(a => a.status === 'pending');
        for (const action of pending) {
            try {
                await this.sendToServer(action);
                action.status = 'synced';
                action.syncedAt = new Date().toISOString();
            } catch (error) {
                action.status = 'failed';
                action.error = error.message;
            }
        }
        this.saveToLocalStorage();
    }
}
8. ВЕБ-ИНТЕРФЕЙС
8.1. Структура страниц
Дашборд (зависит от роли)
Бухгалтер: Последние УПД, УПД требующие распределения

HR_MANAGER: Табели на проверке, Статистика по бригадам

EQUIPMENT_MANAGER: Новые заявки на технику, Заявки без указанной стоимости

MATERIALS_MANAGER: Новые заявки на материалы, УПД для распределения

Руководитель: Сводка по объектам, Графики освоения бюджета, Алерты

Администратор: Статус системы, Последние ошибки, Активность пользователей

Объекты учета
Таблица объектов с фильтрами и поиском

Карточка объекта: основная информация, затраты по типам, графики

Кнопки: Редактировать, Экспорт отчета, Архивировать

Табели РТБ
Таблица табелей с фильтрами (статус, период, бригадир)

Детальный просмотр табеля с возможностью редактирования

Интерфейс указания ставки за час

Экспорт в Excel

Аренда техники/инструмента
Календарь занятости техники

Таблица заявок с фильтрами

Форма указания стоимости (ставка за час, галочки доставки/штрафа)

Интерфейс привязки поставщика

Заявки на материалы
Таблица заявок со статусами

Детальный просмотр заявки с историей изменений

Интерфейс согласования/отклонения

Привязка УПД к заявке

УПД и распределение
Матричный интерфейс распределения строк УПД

Предпросмотр распарсенных данных

История распределения

Поиск по номеру УПД, поставщику, дате

Аналитика и отчеты
Структура затрат по объектам (круговые диаграммы)

Динамика затрат по месяцам (линейные графики)

Использование техники (столбчатые диаграммы)

Формирование периодических отчетов (квартал, полугодие, год)

8.2. Компоненты интерфейса
Матрица распределения УПД
typescript
interface UPDistributionMatrix {
  updId: string;
  rows: UPDistributionRow[];
}

interface UPDistributionRow {
  productName: string;
  quantity: number;
  price: number;
  amount: number;
  requestId: string | null;  // Выбранная заявка
  objectId: string;          // Автоматически из заявки
  distributedQuantity: number; // Сколько распределено
}
Форма указания стоимости аренды
typescript
interface EquipmentCostForm {
  equipmentOrderId: string;
  actualHours: number;
  hourRate: number;
  includeDelivery: boolean;
  deliveryAmount: number;
  includePenalty: boolean;
  penaltyAmount: number;
  totalAmount: number; // Авторасчёт
  supplier: string;
}
Интерфейс проверки табеля
typescript
interface TimeSheetReview {
  timeSheetId: string;
  items: TimeSheetItem[];
  totalHours: number;
  calculatedAmount: number;
  hourRate: number;
  status: 'pending' | 'corrected' | 'approved';
  corrections: Correction[];
}
9. БЕЗОПАСНОСТЬ И НАДЕЖНОСТЬ
9.1. Меры безопасности
Аутентификация: JWT токены с refresh

Авторизация: Ролевая модель (RBAC) с проверкой на каждом эндпоинте

Валидация: Pydantic схемы на входе, санитизация данных

Защита от атак: Rate limiting, SQL injection protection, XSS protection

Шифрование: HTTPS для всех соединений

9.2. Резервное копирование и восстановление
Бэкапы: Автоматическое ежедневное резервное копирование БД

Хранение: 30 дней истории бэкапов

Восстановление: Процедура восстановления из бэкапа за 4 часа

Мониторинг: Проверка целостности бэкапов

9.3. Мониторинг и логирование
Метрики: Prometheus + Grafana (запросы в секунду, время ответа, ошибки)

Логи: Централизованное логирование всех действий

Алерты: Уведомления о критических ошибках, падении сервисов

Аудит: Полная история изменений всех сущностей

10. ТРЕБОВАНИЯ К ИНФРАСТРУКТУРЕ
10.1. Серверные требования
Ресурс	Требование	Обоснование
CPU	4+ ядра	Обработка XML, одновременная работа пользователей
RAM	8+ GB	Кэширование данных, работа СУБД
Storage	100+ GB SSD	Хранение файлов УПД, бэкапов
Network	Статический IP, SSL	Доступ из интернета, безопасность
OS	Ubuntu 20.04/22.04 LTS	Стабильность, поддержка
10.2. Оценка нагрузки
Пользователи: До 100 одновременных

УПД в месяц: До 150 документов

Заявки в месяц: До 300 заявок (материалы + техника)

Табели в месяц: До 50 бригад × 4 недели = 200 табелей

10.3. Docker конфигурация
yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: cost_management
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
  
  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
  
  minio:
    image: minio/minio
    environment:
      MINIO_ROOT_USER: ${MINIO_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD}
    volumes:
      - minio_data:/data
  
  backend:
    build: ./backend
    depends_on:
      - postgres
      - redis
      - minio
    environment:
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@postgres/cost_management
      REDIS_URL: redis://redis:6379/0
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_TOKEN}
    ports:
      - "8000:8000"
  
  telegram-bot:
    build: ./telegram-bot
    depends_on:
      - backend
      - redis
    environment:
      API_URL: http://backend:8000
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_TOKEN}
  
  frontend:
    build: ./frontend
    environment:
      VITE_API_URL: http://localhost:8000/api
    ports:
      - "3000:3000"
  
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - backend
      - frontend
11. ПЛАН ВНЕДРЕНИЯ
Фаза 1: Подготовка (2 недели)
Настройка инфраструктуры (сервер, домен, SSL)

Развертывание базовых сервисов (PostgreSQL, Redis, MinIO)

Настройка CI/CD пайплайна

Создание базовых моделей и миграций

Фаза 2: Ядро системы (4 недели)
Аутентификация и система ролей

Модели объектов, пользователей, бригад

Базовый веб-интерфейс (дашборд, объекты)

Telegram бот (регистрация, главное меню)

Фаза 3: Учет времени бригад (3 недели)
Модели табелей РТБ и бригад

Интерфейс подачи табелей через бот (Excel + пошаговый)

Веб-интерфейс проверки и утверждения табелей

Расчет ФОТ и интеграция с отчетами

Фаза 4: Аренда техники (3 недели)
Модели заявок на аренду

Процесс заявки через бот

Веб-интерфейс управления арендой

Учет стоимости и штрафов

Фаза 5: Материалы и УПД (4 недели)
Модели заявок на материалы

Загрузка и парсинг XML УПД

Матричный интерфейс распределения УПД

Интеграция заявок и УПД

Фаза 6: Аналитика и отчеты (2 недели)
Сводные отчеты по объектам

Графики и диаграммы

Экспорт в Excel/PDF

Периодические отчеты

Фаза 7: Тестирование и оптимизация (2 недели)
Интеграционное тестирование

Нагрузочное тестирование

Исправление ошибок

Оптимизация производительности

Фаза 8: Внедрение и обучение (1 неделя)
Развертывание на продакшн

Миграция тестовых данных

Обучение пользователей

Запуск в промышленную эксплуатацию

12. КРИТЕРИИ ПРИЕМКИ
12.1. Функциональные требования
Регистрация и аутентификация пользователей

Привязка Telegram аккаунтов

Управление объектами учета

Подача табелей РТБ через бот (Excel + пошагово)

Проверка и утверждение табелей в веб-интерфейсе

Расчет ФОТ на основе ставок

Создание заявок на аренду техники через бот

Утверждение заявок, указание поставщика

Учет стоимости аренды (ставка, доставка, штрафы)

Создание заявок на материалы через бот

Согласование заявок на материалы

Загрузка и парсинг XML УПД

Распределение УПД по заявкам и объектам

Формирование отчетов по объектам

Экспорт данных в Excel/PDF

Настройка уведомлений в профиле

12.2. Нефункциональные требования
Время отклика API < 200 мс (p95)

Доступность системы > 99.5%

Поддержка 100+ одновременных пользователей

Обработка до 150 УПД в месяц

Резервное копирование данных

Полное аудит-логирование

Мобильная адаптация веб-интерфейса

12.3. Бизнес-метрики
Сокращение времени обработки документов на 50%

Увеличение прозрачности расходов на 80%

Сокращение ошибок учета на 70%

Удовлетворенность пользователей > 90%

13. РИСКИ И МИТИГАЦИЯ
13.1. Технические риски
Риск	Вероятность	Влияние	Меры митигации
Сложность парсинга XML УПД	Высокая	Высокое	Ручной ввод как fallback, поэтапный парсинг
Интеграция Telegram бота	Средняя	Среднее	Очередь сообщений, офлайн-режим
Производительность с ростом данных	Низкая	Высокое	Индексы, кэширование, пагинация
Безопасность данных	Средняя	Высокое	Регулярные аудиты, шифрование
13.2. Бизнес-риски
Риск	Вероятность	Влияние	Меры митигации
Сопротивление пользователей	Высокая	Среднее	Поэтапное внедрение, обучение, поддержка
Изменение требований	Средняя	Среднее	Гибкая архитектура, регулярные демо
Юридические требования	Низкая	Высокое	Консультации с юристами, аудит логов
Зависимость от ключевых разработчиков	Низкая	Высокое	Документация, кросс-тренинг
14. ГЛОССАРИЙ
14.1. Термины
Объект учета - Стройплощадка, проект строительства

Затрата - Любой расход денежных средств компании

УПД - Универсальный передаточный документ

РТБ - Рабочий табель бригадира (учет времени бригады)

Бригадир/Прораб - Полевой сотрудник, руководитель бригады

Заявка на технику - Запрос на аренду спецтехники

Заявка на материалы - Запрос на поставку строительных материалов

Инертные материалы - Сыпучие материалы (песок, щебень, ПГС)

14.2. Коды и номера
Номер объекта: OBJ-2024-001

Номер табеля РТБ: РТБ-001 (сброс в начале года)

Номер заявки на технику: EQ-2024-001

Номер заявки на материалы: MR-2024-001

Номер УПД: Берется из документа поставщика

15. ПРИЛОЖЕНИЯ
15.1. Формат Excel-шаблона для табелей РТБ
text
Файл: РТБ-001_Иванов_20-26_января_2024.xlsx

Заголовок:
Бригадир: Иванов Иван Иванович
Период: с 20.01.2024 по 26.01.2024
Номер табеля: РТБ-001

Таблица:
| ФИО          | Дата       | Объект          | Часы |
|--------------|------------|-----------------|------|
| Петров П.П.  | 20.01.2024 | ЖК "Северный"   | 8    |
| Петров П.П.  | 21.01.2024 | ЖК "Северный"   | 8    |
| Сидоров С.С. | 20.01.2024 | ЖК "Южный"      | 6    |
| Сидоров С.С. | 20.01.2024 | Склад           | 2    |
15.2. Пример XML УПД для парсинга
xml
<?xml version="1.0" encoding="UTF-8"?>
<Файл>
  <СвУчДокОбор>
    <ИдОтпр>ПоставщикООО</ИдОтпр>
    <ИдПол>НашаКомпания</ИдПол>
  </СвУчДокОбор>
  <Документ>
    <НомерСчФ>12345</НомерСчФ>
    <ДатаСчФ>2024-01-20</ДатаСчФ>
    <ТаблСчФакт>
      <СведТов>
        <НомСтр>1</НомСтр>
        <НаимТов>Кабель ВВГ 3x2.5</НаимТов>
        <КолТов>100</КолТов>
        <ЦенаТов>150.00</ЦенаТов>
        <СтТовБезНДС>15000.00</СтТовБезНДС>
      </СведТов>
    </ТаблСчФакт>
  </Документ>
</Файл>
15.3. Матрица распределения УПД (скриншот)
text
УПД №12345 от "ПоставщикООО" (20.01.2024) - 25,000 ₽

┌─────────────────────────────────────────────┬─────────────────┬────────────┐
│ Материал               │ Количество │ Цена  │ Заявка         │ Объект      │
├─────────────────────────────────────────────┼─────────────────┼────────────┤
│ Кабель ВВГ 3x2.5       │ 100 м      │ 150 ₽ │ MR-2024-001 ▼  │ Северный    │
│ Муфты кабельные        │ 5 шт       │ 200 ₽ │ MR-2024-002 ▼  │ Южный       │
│ Песок речной           │ 20 м³      │ 800 ₽ │ MR-2024-001 ▼  │ Северный    │
└─────────────────────────────────────────────┴─────────────────┴────────────┘

[РАСПРЕДЕЛИТЬ ВЫБРАННОЕ] [РАСПРЕДЕЛИТЬ ВСЁ] [СОХРАНИТЬ ЧЕРНОВИК]

16. УТОЧНЕНИЯ И РЕШЕНИЯ (РЕЗУЛЬТАТЫ ТРЕБОВАНИЙ)

16.1. Блок 1: Рабочее время бригад (РТБ)

Q1.1 - Сроки подачи табелей
✅ РЕШЕНИЕ: Табель можно подавать в любой день недели. Предпочтительно — пятница.
- Система не ограничивает день подачи
- Напоминания настроены для стимуляции к пятнице

Q1.2 - Расчет заработной платы
✅ РЕШЕНИЕ: Расчет ведется строго по фактическим часам БЕЗ дневного минимума.
- Каждый час оплачивается по ставке, установленной HR_MANAGER
- Если сотрудник отработал 3 часа — оплачиваются 3 часа
- Дневной минимум часов отсутствует

Q1.3 - График напоминаний о табелях
✅ РЕШЕНИЕ: Трехуровневая система напоминаний через Telegram
1. Пятница, 15:00 — первое напоминание
2. Пятница, 17:00 — повторное напоминание (если не отправил)
3. Суббота, 13:00 — финальное напоминание перед сроком

16.2. Блок 2: Аренда техники и инструмента

Q2.1 - Многообъектные заказы
✅ РЕШЕНИЕ: Один заказ техники может охватывать НЕСКОЛЬКО объектов одновременно.
- Для каждого объекта отслеживаются отдельные часы работы
- Система автоматически ведет разделение по объектам
- Пример: экскаватор работает на Северном объекте 40 часов и на Южном 30 часов в одном заказе

Q2.2 - Распределение затрат
✅ РЕШЕНИЕ: Система автоматически рассчитывает и распределяет затраты.
- EQUIPMENT_MANAGER указывает часовую ставку
- Система умножает: (часы объекта 1 × ставка) + (часы объекта 2 × ставка)
- Автоматическое распределение суммы по объектам учета

Q2.3 - Штрафы и пени
✅ РЕШЕНИЕ: Штрафы — индивидуальные фиксированные суммы, вводит менеджер вручную.
- Штрафы НЕ рассчитываются автоматически
- EQUIPMENT_MANAGER самостоятельно решает, есть ли штраф и какой размер
- Штраф добавляется отдельной строкой в расчет затрат

16.3. Блок 3: Заявки на материалы и инертные материалы

Q3.1 - Срочность заявок
✅ РЕШЕНИЕ: Срочность — только информационный признак (не автоматизирован).
- Бригадир указывает срочность при создании заявки (обычная / срочная)
- Система НЕ генерирует автоматических напоминаний по срочности
- Срочность видна менеджеру как справка для приоритизации

Q3.2 - Согласование заявок
✅ РЕШЕНИЕ: PROCUREMENT_MANAGER имеет право ОДОБРИТЬ или ОТКЛОНИТЬ заявку.
- Одна роль = две возможности
- При одобрении заявка переходит в статус "В ОБРАБОТКЕ"
- При отклонении — статус "ОТКЛОНЕНА" с комментарием причины

Q3.3 - Частичные поставки материалов
✅ РЕШЕНИЕ: Заявка остается ОТКРЫТОЙ, пока не поставлены ВСЕ позиции.
- При привязке первого УПД статус меняется на "ЧАСТИЧНО ПОСТАВЛЕНО"
- Заявка закрывается только когда доставлено 100% всех позиций
- Менеджер отслеживает недостающие позиции

Q3.4 - Инертные материалы с обязательным временем доставки
✅ РЕАЛИЗОВАНО: Добавлена поддержка инертных материалов (песок, щебень, раствор).
- Тип материала: enum MaterialType ('regular' | 'inert')
- Поле delivery_time: VARCHAR(50), обязательно при material_type='inert'
- Валидация на уровне Pydantic: field_validator проверяет наличие delivery_time для инертных
- Миграция: add_material_type.py добавляет поля material_type и delivery_time в material_requests
- Тесты: test_inert_validation.py проверяет все сценарии (3/3 успешно)

16.4. Блок 4: Универсальные передаточные документы (УПД)

Q4.1 - Обязательность распределения
✅ РЕШЕНИЕ: ВСЕ УПД ОБЯЗАТЕЛЬНО должны быть отнесены к объектам учета.
- Система НЕ позволяет оставить УПД без привязки к объекту
- Каждая строка УПД → минимум один объект (через заявку или напрямую)
- Остаток на конец месяца НЕ ДОПУСКАЕТСЯ

Q4.2 - Распределение строки УПД на несколько получателей
✅ РЕШЕНИЕ: Одна строка УПД может распределяться на НЕСКОЛЬКО заявок И объектов одновременно.
- Пример: 100 м кабеля → 60 м на Северный объект, 40 м на Южный объект
- В матричном интерфейсе распределения менеджер указывает количество для каждого объекта
- Автоматический расчет суммы пропорционально количеству

Q4.3 - Прямое распределение товара без заявки
✅ РЕШЕНИЕ: МАТЕРИАЛЫ MANAGER может привязать товар напрямую к объекту БЕЗ ПРИВЯЗКИ К ЗАЯВКЕ.
- Решение принимает MATERIALS_MANAGER на свое усмотрение
- Используется для внеплановых поступлений, подарков, переводов между объектами
- Заявка не требуется, объект указывается вручную

16.5. Блок 5: Роли и доступ

Q5.1 - Видимость объектов для FOREMAN
✅ РЕШЕНИЕ: Двухуровневая видимость для бригадира (FOREMAN).
- ВИДИТ ВСЕ активные объекты компании (все строящиеся объекты)
- НО видит только СВОИ заявки на материалы и технику
- Все остальные роли видят ВСЕ объекты И ВСЕ заявки

Q5.2 - Суммирование прав при множественных ролях
✅ РЕШЕНИЕ: При назначении пользователю нескольких ролей права СУММИРУЮТСЯ без конфликтов.
- Пример: если человек одновременно EQUIPMENT_MANAGER и MATERIALS_MANAGER
- То он может утверждать и технику И материалы
- Конфликтов между правами нет (система не использует вычитание прав)
- Пользователь получает объединение возможностей всех своих ролей

16.6. Блок 6: Telegram-бот и уведомления

Q6.1 - Режим работы без интернета
✅ РЕШЕНИЕ: Система ОТКАЗЫВАЕТСЯ от офлайн-режима в целях упрощения.
- Система требует постоянного соединения с интернетом
- Все операции в боте выполняются только онлайн
- Синхронизация действий при восстановлении связи НЕ РЕАЛИЗУЕТСЯ
- Обоснование: снижение сложности разработки и поддержки

Q6.2 - Система уведомлений
✅ РЕШЕНИЕ: Уведомления НА 100% ролевые, ТОЛЬКО через Telegram, внешних сервисов НЕТ.
- Каждая роль получает уведомления только о своих типах операций
  - EQUIPMENT_MANAGER: только о заявках на технику и изменениях в статусе
  - PROCUREMENT_MANAGER: только о заявках на материалы
  - HR_MANAGER: только о табелях на проверку
  - Итд.
- Канал доставки: ТОЛЬКО Telegram
- ИСКЛЮЧЕНЫ: SMS, Email, push-уведомления, внешние сервисы
- Многороли: пользователь с двумя ролями получает уведомления ОТ ОБЕИХ ролей

16.7. Блок 7: Архивирование и отчетность

Q7.1 - Стратегия архивирования объектов
✅ РЕШЕНИЕ: Комплексный процесс закрытия и архивирования объектов.

Правила архивирования:
- В архив попадают ТОЛЬКО ЗАВЕРШЕННЫЕ объекты (когда все работы окончены)
- Объекты НЕЛЬЗЯ архивировать, если они еще в работе

Процесс закрытия объекта (2 этапа):
1. MANAGER (Руководитель) инициирует запрос на закрытие объекта
2. PROCUREMENT_MANAGER (Менеджер по закупкам) подтверждает или отклоняет запрос
   - Если подтвердил: объект переходит в статус "ЗАКРЫТ"
   - Если отклонил: возвращается к MANAGER с причиной отклонения

Автоматическая архивация:
- Дата: 29 декабря каждого года (один раз в год)
- Действие: Все ЗАКРЫТЫЕ объекты автоматически архивируются
- Место: Переводятся в архивное хранилище
- Доступ: Архивные объекты доступны только для просмотра и экспорта отчетов

Отчеты при архивировании:
- Полный отчет: Все данные по объекту за весь период (табели, УПД, заявки, затраты)
- Точечный отчет: Сводная информация по объекту за период работы (финансовый итог, статистика)

Q7.2 - Формат и содержание экспортируемых отчетов
✅ РЕШЕНИЕ: Отчеты экспортируются в формат EXCEL со ВСЕМИ данными без исключений.

Формат файла: Microsoft Excel (.xlsx)
Содержание: ВСЕ данные — полная выгрузка
- Информация об объекте (название, контракт, адрес, даты)
- ВСЕ табели РТБ по объекту (с расчетом ФОТ)
- ВСЕ заявки на материалы (с историей изменений статусов)
- ВСЕ приложенные УПД и их распределение
- ВСЕ заявки на технику (с указанием поставщиков и стоимости)
- ВСЕ затраты (материалы, техника, ФОТ) с разбором по статьям
- Финансовый итог по объекту и по видам затрат
- История изменений (кто, когда, что изменил)

Структура листов в Excel:
- Лист 1: Сводка по объекту (основная инфо)
- Лист 2: Табели РТБ
- Лист 3: Заявки на материалы
- Лист 4: УПД и распределение
- Лист 5: Заявки на технику
- Лист 6: Финансовая сводка
- Лист 7: История операций (аудит)

16.8. Блок 8: Внедрение системы

Q8.1 - Стратегия миграции данных
✅ РЕШЕНИЕ: Миграция ИСТОРИЧЕСКИХ данных НЕ ПРОВОДИТСЯ.

Подход:
- Старые данные остаются в текущих системах (Excel, 1С, бумаге)
- Система НАЧИНАЕТ РАБОТУ с первого НОВОГО объекта
- Граница: с момента развертывания системы все новые объекты и данные заносятся в систему
- Архив: Исторические закрытые объекты остаются в старых системах для справки

Преимущества:
- Быстрое внедрение (нет работ по конвертации данных)
- Чистая история в новой системе
- Избегаем ошибок трансформации данных
- Старые системы остаются единым источником истории

Q8.2 - Пилотное тестирование (перед полным запуском)
✅ РЕШЕНИЕ: Месячный пилот на реальных объектах со ВСЕМИ ролями.

Участники пилота:
- ВСЕ роли должны быть распределены между сотрудниками компании
- Как минимум один человек на каждую роль (или несколько на критичные)
- Рекомендуется: 8 пилот-пользователей (по одному на основные роли)

Объекты тестирования:
- РЕАЛЬНЫЕ строящиеся объекты (не тестовые, не синтетические данные)
- 2-3 активных объекта для полноты данных
- На этих объектах ведется реальная работа во время пилота

Длительность пилота:
- РОВНО 1 месяц (4 недели)
- Период: Полный цикл одного месяца (от начала до 29.12 архивации)
- После месяца: анализ результатов и решение о переходе на production

Критерии успеха (все функции должны работать):
✅ РТБ - табели: создание, подача, проверка, утверждение, расчет ФОТ, напоминания
✅ Техника: создание заявок, утверждение, указание поставщика, расчет стоимости, штрафы
✅ Материалы: создание заявок, согласование, обработка, частичные поставки
✅ УПД: загрузка, парсинг, распределение (включая многостроковое и прямое)
✅ Отчеты: экспорт в Excel со всеми данными
✅ Архивирование: закрытие объектов (инициирование и подтверждение), автоматическая архивация
✅ Уведомления: доставка через Telegram по ролям
✅ Видимость: FOREMAN видит все объекты, но свои заявки; остальные видят всё
✅ Многороли: если пилотер имеет несколько ролей, все права работают корректно

Критерий финального успеха пилота:
- "УСПЕХ" = 100% функциональность, 0 критичных ошибок, приемлемая скорость (< 200мс)
- Если есть критичные баги → доработка → новый цикл пилота
- Если всё работает → переход на production (full rollout)

16.9. Парсинг УПД (XML) — детальная спецификация

Анализ реальных файлов УПД: 15 документов от разных поставщиков

Формат и характеристики входящих файлов УПД:
- Формат: XML
- Кодировка: windows-1251 (кириллица) — ОБЯЗАТЕЛЬНО
- Версия формата: 5.01 и 5.03
- Структура: Иерархическая с вложенными элементами на русском языке
- Генераторы: Elewise LegalDoc 2.1, VO2_xslt, Diadoc 1.0, 1С:Бухгалтерия

МАТРИЦА СТРУКТУРНЫХ ВАРИАНТОВ (по результатам анализа 15 файлов):

Тип 1: Elewise LegalDoc (версия 5.01) с обёрткой СчетФактура
────────────────────────────────────────────────────────────────
Структура: Файл → Документ → СчетФактура → Таблица → Товар
Генератор: Elewise LegalDoc 2.1
Версия: 5.01
Файлы (9 документов):
  - 2024-07-24 (2 товара)
  - 2024-08-06 (12 товаров) ← самый крупный Type 1
  - 2024-08-24 (1 товар)
  - 2024-10-11 (17 товаров)
  - [Дополнительные из предыдущего пакета]

Характеристики товаров:
  - Диапазон: 1-17 товарных позиций в одном документе
  - НДС: 20% (единообразно)
  - Типы товаров: кабели, провода, электроборудование, материалы
  - Единицы: м (метры), м³, кг, шт (штуки)

XPath для извлечения (Type 1):
```
Номер документа:     //Документ/СчетФактура/СчЧастьСч/@НомерСчФ
Дата документа:      //Документ/СчетФактура/СчЧастьСч/@ДатаСчФ
Поставщик:           //Документ/СчетФактура/СчЧастьСч/Грузоотправитель/Лицо/НаимЮЛ
ИНН поставщика:      //Документ/СчетФактура/СчЧастьСч/Грузоотправитель/Лицо/@ИНН
Товары:              //Документ/СчетФактура/Таблица/Товар
  Название:         Товар/Сведения/@НаимТов
  Количество:       Товар/@КолТов
  Единица:          Товар/@ЕдИзм
  Цена:             Товар/@ЦенаТов
  Сумма без НДС:    Товар/@СтТовБезНДС
  НДС ставка:       Товар/@СтавкаНДС (обычно 20%)
  НДС сумма:        Товар/@СумНДС
Итого без НДС:       //Документ/СчетФактура/Таблица/ИтогСчФакт/@ДокСумма
Итого НДС:           //Документ/СчетФактура/Таблица/ИтогСчФакт/@СумНДС
ИТОГО с НДС:         //Документ/СчетФактура/Таблица/ИтогСчФакт/@СуммаВсего
```

Тип 2: Elewise LegalDoc (версия 5.03) БЕЗ СчетФактура
────────────────────────────────────────────────────────────────
Структура: Файл → Документ → (прямо товары в Таблица)
Генератор: Elewise LegalDoc 2.1
Версия: 5.03
Файлы (6 документов):
  - 2025-12-11 (19 товаров) ← самый крупный заказ
  - 2026-01-22 (16 товаров, НДС 22%)
  - [4 дополнительных документа из нового пакета]

Характеристики товаров:
  - Диапазон: 1-19 товарных позиций в одном документе
  - НДС: 20% и 22% (вариативно!)
  - Типы товаров: кабели, трансформаторы, светильники, УЗО, провода
  - Единицы: м, кг, л, шт, шт.

XPath для извлечения (Type 2):
```
Номер документа:     //Документ/@НомерДокумента ИЛИ //Документ/СчЧастьСч/@НомерСчФ
Дата документа:      //Документ/@ДатаДокумента ИЛИ //Документ/СчЧастьСч/@ДатаСчФ
Поставщик:           //Документ/Продавец/Лицо/НаимЮЛ ИЛИ //Документ/СчЧастьСч/Лицо
ИНН поставщика:      //Документ/Продавец/Лицо/@ИНН
Товары:              //Документ/Таблица/Товар
  Название:         Товар/@НаимТов
  Количество:       Товар/@КолТов
  Единица:          Товар/@ЕдИзм
  Цена:             Товар/@ЦенаТов
  Сумма без НДС:    Товар/@СтТовБезНДС
  НДС ставка:       Товар/@СтавкаНДС (20% или 22%)
  НДС сумма:        Товар/@СумНДС
Итого без НДС:       //Документ/Таблица/ИтогДокумента/@ДокСумма
Итого НДС:           //Документ/Таблица/ИтогДокумента/@СумНДС
ИТОГО с НДС:         //Документ/Таблица/ИтогДокумента/@СуммаВсего
```

СВОДНАЯ ТАБЛИЦА ВСЕХ 15 ФАЙЛОВ:

| № | Дата       | Версия | Генератор           | Товаров | НДС   | Тип     |
|---|------------|--------|---------------------|---------|-------|---------|
| 1 | 2024-07-24 | 5.01   | Elewise LegalDoc 2.1| 2       | 20%   | Type 1  |
| 2 | 2024-08-06 | 5.01   | Elewise LegalDoc 2.1| 12      | 20%   | Type 1  |
| 3 | 2024-08-24 | 5.01   | Elewise LegalDoc 2.1| 1       | 20%   | Type 1  |
| 4 | 2024-10-11 | 5.01   | Elewise LegalDoc 2.1| 17      | 20%   | Type 1  |
| 5 | 2025-12-11 | 5.03   | Elewise LegalDoc 2.1| 19      | 20%   | Type 2  |
| 6 | 2026-01-22 | 5.03   | Elewise LegalDoc 2.1| 16      | 22%   | Type 2  |
| 7 | [previous] | 5.03   | VO2_xslt            | 2       | 20%   | Type 3  |
| 8 | [previous] | 5.03   | VO2_xslt            | 1       | 20%   | Type 3  |
| 9 | [previous] | 5.03   | VO2_xslt            | 1       | 20%   | Type 3  |
|10 | [previous] | 5.03   | VO2_xslt            | 6       | 20%   | Type 3  |
|11 | [previous] | 5.03   | Diadoc 1.0          | 1       | 20%   | Type 3  |
|12 | [previous] | 5.01   | 1С:Бухгалтерия      | 1       | 20%   | Type 1* |
|13 | [previous] | 5.01   | 1С:Бухгалтерия      | 2       | 20%   | Type 1* |
|14 | [previous] | 5.03   | 1С:Бухгалтерия      | ?       | ?     | Type 2* |
|15 | [previous] | 5.03   | Diadoc 1.0          | ?       | ?     | Type 3* |

* Обозначены по сведениям из предыдущего анализа

КРИТИЧНЫЕ ВАРИАЦИИ:

1. НДС ставки: 20%, 22%, 0% (найдены все три в реальных документах)
2. Количество товаров: 1-19 позиций
3. Версии XML: 5.01 и 5.03 (функционально идентичны, структурно отличаются)
4. Структура обёртки: 
   - С СчетФактура (Type 1)
   - Без СчетФактура (Type 2-3)

РЕКОМЕНДУЕМАЯ ЛОГИКА ПАРСЕРА:

```python
import xml.etree.ElementTree as ET
from typing import Dict, List, Optional

def parse_upd_xml(file_path: str) -> Dict:
    """
    Парсит УПД XML файл (Type 1, 2 или 3) и возвращает структурированные данные
    
    Поддерживает:
    - Версии 5.01 и 5.03
    - Генераторы: Elewise, VO2_xslt, Diadoc, 1С:Бухгалтерия
    - НДС ставки: 20%, 22%, 0%
    - Товары: 1-19 позиций
    """
    
    # 1. Декодировать из windows-1251
    with open(file_path, 'r', encoding='windows-1251') as f:
        content = f.read()
    
    root = ET.fromstring(content)
    
    # 2. Определить тип структуры (Type 1 vs Type 2/3)
    has_schet_faktura = root.find('.//СчетФактура') is not None
    
    # 3. Выбрать правильный XPath в зависимости от типа
    if has_schet_faktura:
        # Type 1: Elewise 5.01
        doc_elem = root.find('.//СчетФактура/СчЧастьСч')
        tabletka = root.find('.//СчетФактура/Таблица')
        doc_number = doc_elem.get('НомерСчФ')
        doc_date = doc_elem.get('ДатаСчФ')
    else:
        # Type 2/3: Elewise 5.03, VO2_xslt, Diadoc
        doc_elem = root.find('.//Документ')
        tabletka = root.find('.//Таблица')
        doc_number = doc_elem.get('НомерДокумента') or doc_elem.find('.//СчЧастьСч').get('НомерСчФ')
        doc_date = doc_elem.get('ДатаДокумента') or doc_elem.find('.//СчЧастьСч').get('ДатаСчФ')
    
    # 4. Извлечь товары (может быть в разных местах)
    items = []
    for tovar_elem in tabletka.findall('.//Товар'):
        item = {
            'product_name': tovar_elem.get('НаимТов') or tovar_elem.find('Сведения').get('НаимТов'),
            'quantity': float(tovar_elem.get('КолТов', 0)),
            'unit': tovar_elem.get('ЕдИзм'),
            'unit_price': float(tovar_elem.get('ЦенаТов', 0)),
            'sum_without_vat': float(tovar_elem.get('СтТовБезНДС', 0)),
            'vat_rate': float(tovar_elem.get('СтавкаНДС', '20').rstrip('%')),
            'vat_amount': float(tovar_elem.get('СумНДС', 0)),
        }
        items.append(item)
    
    # 5. Вернуть структурированный результат
    return {
        'document_number': doc_number,
        'document_date': doc_date,
        'supplier_name': extract_supplier_name(doc_elem),
        'supplier_inn': extract_supplier_inn(doc_elem),
        'items': items,
        'total_without_vat': float(tabletka.find('.//ИтогСчФакт').get('ДокСумма', 0)),
        'total_vat': float(tabletka.find('.//ИтогСчФакт').get('СумНДС', 0)),
        'total_with_vat': float(tabletka.find('.//ИтогСчФакт').get('СуммаВсего', 0)),
        'item_count': len(items),
    }
```

Критичные требования к парсеру:
✅ Обработка кодировки windows-1251 (ОБЯЗАТЕЛЬНО)
✅ Поддержка русских названий тегов (ОБЯЗАТЕЛЬНО)
✅ Обработка Type 1 (с СчетФактура) и Type 2/3 (без)
✅ Поддержка версий 5.01 и 5.03
✅ НДС ставки: 20%, 22%, 0%
✅ Товары: 1-19 позиций в одном документе
✅ Валидация обязательных полей (номер, дата, товары, суммы)
✅ Преобразование строк в числовые типы (float для сумм, int для количества)
✅ Логирование ошибок парсинга с информацией о файле
✅ Создание записей с статусом NEW в базе данных

Обработка исключений:
- Файл не найден → логирование ошибки
- XML невалидный → попытка восстановления или отклонение
- Кодировка неправильная → автоопределение кодировки (chardet)
- Отсутствуют товары → ошибка валидации (УПД без товаров недопустим)
- Сумма не совпадает → логирование предупреждения (но не блокировка)
- Неизвестный тип структуры → эвристический поиск с логированием
- НДС = 0% → допустимо (льготные/экспортные товары)

16.10. Технические уточнения при реализации

Опции базы данных для реализации уточнений:
```sql
-- Таблица объектов должна содержать статус закрытия
ALTER TABLE cost_objects ADD COLUMN status VARCHAR(50) DEFAULT 'ACTIVE';
-- Возможные значения: ACTIVE, CLOSED_PENDING, CLOSED_APPROVED, ARCHIVED

-- Таблица истории закрытия
CREATE TABLE object_closures (
    id SERIAL PRIMARY KEY,
    object_id INTEGER REFERENCES cost_objects(id),
    requested_by INTEGER REFERENCES users(id),  -- MANAGER
    requested_at TIMESTAMP DEFAULT NOW(),
    confirmed_by INTEGER REFERENCES users(id),  -- PROCUREMENT_MANAGER
    confirmation_status VARCHAR(50),  -- PENDING, APPROVED, REJECTED
    confirmed_at TIMESTAMP,
    rejection_reason TEXT,
    archived_at TIMESTAMP,  -- Автоматически заполняется 29.12
    FOREIGN KEY (requested_by) REFERENCES users(id),
    FOREIGN KEY (confirmed_by) REFERENCES users(id)
);

-- Таблица уведомлений с ролевой фильтрацией
CREATE TABLE notifications (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    roles_triggered VARCHAR(255)[],  -- Массив ролей, которые вызвали уведомление
    notification_type VARCHAR(100),  -- equipment_request, material_request, timesheet, etc
    message_text TEXT,
    delivered_via_telegram BOOLEAN DEFAULT FALSE,
    telegram_message_id VARCHAR(255),
    status VARCHAR(50) DEFAULT 'PENDING',  -- PENDING, SENT, FAILED, READ
    created_at TIMESTAMP DEFAULT NOW(),
    delivered_at TIMESTAMP
);

16.11. Backend: подробная инструкция (без UI)

Назначение блока: описывает серверную часть и правила работы без привязки к внешнему виду.

16.11.1. Состав backend
Сервисы:
- API Gateway: FastAPI (монолит или набор модулей), единая точка входа.
- Парсер УПД: модуль для разбора XML УПД (версии 5.01/5.03).
- Telegram Bot: отдельный сервис (aiogram) с интеграцией через HTTP API.
- Фоновая обработка: очереди/воркеры (по необходимости для уведомлений/экспорта).

Хранилища:
- PostgreSQL: основная транзакционная БД.
- Redis: кэш, rate-limiting, сессии, очереди.
- S3/MinIO: хранение исходных XML УПД и экспортов.

16.11.2. Структура backend-модулей (рекомендуемая)
- auth/: аутентификация, JWT, refresh-токены, RBAC
- users/: профили, роли, уведомления
- objects/: объекты учета
- time_sheets/: табели РТБ
- equipment/: заявки на технику и расчеты
- materials/: заявки на материалы
- upd/: загрузка, парсинг, распределение УПД
- analytics/: отчеты и аналитика
- notifications/: уведомления и Telegram-доставка
- integrations/: внешние интеграции (если появятся)

16.11.3. Жизненный цикл УПД на backend
1) Загрузка XML: файл сохраняется в S3/MinIO.
2) Парсинг: извлекаются поставщик, номер, дата, строки товаров, суммы.
3) Валидация: проверка критичных полей (номер, дата, суммы, строки).
4) Сохранение: документ создается в БД со статусом NEW.
5) Распределение: строки связываются с заявками/объектами.
6) Создание затрат: запись в material_costs и material_cost_items.
7) Завершение: УПД переводится в DISTRIBUTED/ARCHIVED.

16.11.4. Контракты API (backend-only)
Минимальный набор:
- POST /api/v1/material-costs/upload
  Вход: XML-файл, метаданные (опционально)
  Выход: id УПД, статус, распарсенные поля

- GET /api/v1/material-costs/unprocessed
  Выход: список УПД со статусом NEW

- POST /api/v1/material-costs/{id}/distribute
  Вход: массив распределений по строкам (объект/заявка/кол-во/сумма)
  Выход: новый статус и агрегированные суммы

- GET /api/v1/objects/{id}/costs
  Вход: период, типы затрат
  Выход: агрегированные суммы по объекту

16.11.5. Парсер УПД: обязательные правила
- Кодировка: windows-1251 обязательна.
- Поддержка версий: 5.01 и 5.03.
- Поддержка генераторов: Elewise LegalDoc, Diadoc, VO2_xslt, 1С.
- НДС: 0%, 20%, 22%.
- Строки: 1..N (минимум 1 строка).
- Единицы измерения: словарь ОКЕИ + fallback на код.
- Fallback-поля: НДС может быть в НалСт или НДС; номера и даты — в НомерСчФ/НомерДок и ДатаСчФ/ДатаДок.

16.11.6. Трекинг проблем парсинга
Парсер обязан фиксировать проблемы в структуре issues:
- severity: info | warning | error
- element: элемент/атрибут
- message: описание проблемы
- generator: источник (если определен)
- value: найденное значение (если нужно)

Назначение:
- Логировать все нестандартные случаи без падения сервиса.
- Давать материал для расширения поддержки новых генераторов.

16.11.7. Наблюдаемость
- Логи: все ошибки парсинга, распределения, статусов.
- Метрики: количество УПД, среднее время парсинга, доля проблемных файлов.
- Алерты: ошибки парсинга выше порога.

16.11.8. Безопасность backend
- JWT + refresh токены.
- RBAC проверяется на каждом эндпоинте.
- Санитизация входных XML, ограничение размера файлов.
- Rate limit на upload и критичные операции.

16.11.9. Миграции и версии API
- Все изменения БД — через миграции.
- Все новые эндпоинты — через /api/v1 с обратной совместимостью.

16.12. Проблемы, с которыми столкнулись (и решения)

16.12.1. УПД-услуги без КолТов/ЦенаТов
Проблема: УПД с услугами приходили без КолТов/ЦенаТов; парсер создавал строку с 0.
Решение: Если КолТов=0 и ЦенаТов=0 при наличии суммы — трактовать как услугу: qty=1, unit='услуга', price=sum.

16.12.2. Некорректный порядок fallback-парсинга
Проблема: Парсер возвращал пустую позицию, блокируя fallback.
Решение: Возвращать None при отсутствии обязательных атрибутов, чтобы срабатывал fallback.

16.12.3. Формат названия поставщика (1С)
Проблема: 1С использует формат: "ООО ... , ИНН/КПП ..."; имя приходило с лишним хвостом.
Решение: Обрезать часть после запятой и вытащить ИНН регулярным выражением.

16.12.4. НДС в разных атрибутах
Проблема: НДС может быть в НалСт или НДС; часть файлов не парсилась.
Решение: Добавлен fallback: НалСт → НДС → 20%.

16.12.5. Отсутствующий код ОКЕИ 356
Проблема: Единица измерения "час" (код 356) отображалась как "356".
Решение: Добавлен код 356 в словарь ОКЕИ как "ч".

16.12.6. Разные генераторы УПД
Проблема: 4 разных генератора (1С, Elewise, Diadoc, VO2_xslt) использовали разные структуры.
Решение: Введена детекция генератора и гибкая логика извлечения полей.

16.12.7. Неизвестные будущие генераторы
Проблема: риск появления новых форматов.
Решение: Введена система трекинга проблем, чтобы не падать и собирать диагностику.

16.12.8. Кодировка вывода и символы
Проблема: консоль Windows не печатает спецсимволы (✓, ⚠).
Решение: тесты и отчеты используют ASCII-символы в логах.

16.12.9. RecursionError в Pydantic v2 с вложенными моделями
Проблема: При использовании `list[TimeSheetItemCreate]` в TimeSheetCreate Pydantic v2 генерировал RecursionError при попытке построения схемы.
Решение: Заменить вложенные модели на `list[dict[str, Any]]` с описанием структуры в Field(description). Это позволяет избежать циклической зависимости в построении схемы.

16.12.10. Дублирующиеся классы в schemas.py
Проблема: В time_sheets/schemas.py были продублированы классы TimeSheetSubmitRequest, TimeSheetApproveRequest, TimeSheetRejectRequest, причем в дубликатах использовался Optional без импорта.
Решение: Удалены дублирующиеся классы, добавлен импорт Optional в первую строку импортов.

16.12.11. TypeError в UPD парсере (dataclass)
Проблема: В UPDDocument dataclass поля без значений по умолчанию (total_amount, total_vat, total_with_vat) шли ПОСЛЕ полей с дефолтными значениями (supplier_inn, buyer_name и т.д.), что вызывало TypeError.
Решение: Переместить все поля без дефолтов в начало класса, перед полями с Optional[...] = None.

16.13. Критически важные элементы кода (backend)

Назначение: перечисление ключевых компонентов, без которых логика системы некорректна.

16.13.1. Парсер УПД (xml)
- UPDParser.parse(): точка входа парсинга, обязателен try/except и возврат статуса.
- _detect_generator(): определяет генератор и влияет на стратегию извлечения.
- _extract_supplier(): извлекает имя/ИНН поставщика, учитывает формат 1С.
- _extract_items(): выбор правильного парсера строк и fallback.
- _parse_sved_tov(): парсинг стандартных строк товаров.
- _parse_sved_tov_usl_sch(): парсинг строк услуг без КолТов/ЦенаТов.
- convert_okei_to_unit(): маппинг ОКЕИ к человеко-читаемым единицам.
- OKEI_UNITS: словарь кодов единиц измерения (обязателен минимум используемых кодов).
- ParsingIssue + parsing_issues: фиксация проблем без падения сервиса.

16.13.2. API слой
- Контроллеры УПД:
  - upload: прием XML, проверка размера, запись в S3/MinIO.
  - distribute: распределение строк, пересчет сумм, смена статуса.
- Валидация входных данных (Pydantic схемы) для всех endpoint-ов.
- RBAC middleware: контроль доступа по ролям для каждого endpoint-а.
- Аудит-лог: запись изменений критичных сущностей (УПД, заявки, затраты).

16.13.3. Бизнес-логика распределения
- Матрица распределения: один товар → несколько заявок/объектов.
- Валидация сумм: пропорциональный расчет сумм по распределенным количествам.
- Статусы УПД: NEW → DISTRIBUTED → ARCHIVED.

16.13.4. Затраты и агрегаты
- Создание material_costs и material_cost_items по распределению.
- Единая модель cost_entries для агрегированной отчетности.
- Индексы по объектам и датам для отчетов.

16.13.5. Telegram Bot интеграция
- Webhook обработчик: прием событий из бота.
- Состояния FSM для ключевых процессов (табели, заявки).
- Уведомления по ролям через Telegram (без альтернативных каналов).

16.13.6. Безопасность и устойчивость
- Ограничение размера XML, защита от вредоносных payload.
- Контроль кодировки (windows-1251) при чтении XML.
- Централизованное логирование и метрики по парсингу.
- Резервные механизмы (fallback) для НДС, номера, даты.

16.14. Инструкция работы над проектом (процесс выполнения задач)

Назначение: регламент, по которому выполняются доработки backend части проекта.

16.14.1. Принципы
- Без изменения UI (внешний вид не учитывается).
- Изменения только в backend, БД, парсинге, API, бот-интеграции.
- Каждая задача должна иметь: цель, входные/выходные данные, критерии приемки.

16.14.2. Порядок работы
1) Анализ требования
  - Выделить бизнес-цель и модуль (auth/objects/time_sheets/equipment/materials/upd/analytics).
  - Определить сущности БД и API эндпоинты.

2) Проектирование
  - Схемы БД: таблицы/поля/связи/индексы.
  - Контракты API: вход/выход, статусы, ошибки.
  - Бизнес-логика: статусы, переходы, валидаторы.

3) Реализация
  - Миграции БД.
  - Модели/репозитории/сервисы.
  - Контроллеры API.
  - Интеграции (Telegram, S3, очереди) при необходимости.

4) Проверки
  - Юнит-тесты бизнес-логики.
  - Интеграционные тесты ключевых сценариев.
  - Регрессия парсинга УПД на наборе реальных файлов.

5) Наблюдаемость
  - Логи ошибок и проблем парсинга.
  - Метрики по времени парсинга и количеству ошибок.

6) Документация
  - Обновить этот файл (Система.md) по итогам.
  - Зафиксировать проблемы и решения (раздел 16.12).

16.14.3. Обязательные артефакты каждой задачи
- Описание задачи (цель, результат, ограничения).
- Список измененных таблиц БД.
- Список измененных API эндпоинтов.
- Сценарии тестирования и фактический результат.

16.14.4. Специфично для УПД
- Любые изменения парсера сопровождаются обновлением:
  - правил парсинга (16.11.5)
  - трекинга проблем (16.11.6)
  - списка проблем и решений (16.12)
- Обязательный прогон на наборе реальных XML (минимум 15 файлов).

16.14.5. Критерии завершения задачи
- Все тесты проходят.
- Новые поля/статусы отражены в API контрактах.
- Нет регрессий по парсеру УПД.
- В документе Система.md отражены все изменения.
```